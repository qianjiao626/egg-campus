# 蛋蛋世界：腾讯云 MySQL 数据库建设计划

## 2026-08-28 全功能测试、即时整改与版本边界

### 本轮收口证据（2026-08-28）

- 真人式浏览器走查：全功能入口已覆盖，未使用 agent-browser、未截图；退出序列 `refresh 200 -> logout 200`，退出后 17 秒无盲盒轮询。
- 隔离真实写入 E2E：`25/25`，仅 `dandan_campus_test`，清理 `3 users`、`7 point transactions`、`0 attachments`。
- 白屏结构：主页面 `<div>` 开闭 `1362/1362`，`#stage` 唯一，初始化查询 `2` 处。
- 发布边界：r2 manifest 覆盖完整前端运行时集合；staging、依赖完整性、生产门禁、备份指针与失败回滚测试已通过。
- 生产状态：r2 仍为 `isolated-test-only` / `productionDeployable=false`；未上传生产，未 reload Nginx，等待明确生产批准。

| 项目 | 当前证据 | 状态 |
|---|---|---|
| 真人式浏览器走查 | 全新会话完成注册、登录、资料、任务广场、排行榜、通知、打听、盲盒、反馈、移动端与退出回归；无截图、未用 agent-browser | completed |
| 跨账号完整业务闭环 | 隔离库真实写入 E2E 25/25，覆盖三用户、管理员、任务、协商取消、邀请、打听、盲盒、好友、排行榜、WSS 与 REST 兜底 | completed |
| 自动化与构建 | 53 个测试文件、315/315；TypeScript build 通过 | completed |
| 白屏结构回归 | `<div>` 1362/1362，`#stage` 唯一且初始化查询存在 | completed |
| 版本边界整改 | r1 标记 superseded；r2 为 isolated-test-only；生产部署要求 production-approved 和 productionDeployable=true | completed |
| 部署门禁 | 显式 RELEASE_MANIFEST、默认 `/var/www/dd`、复制前生产校验；Git Bash 语法检查通过 | completed |
| 生产发布 | 当前 r2 不具备生产批准；本轮未上传、未 reload Nginx | blocked_by_release_approval |

### 当前下一步

如需生产发布，必须先由负责人将经审计的版本明确提升为 `production-approved` 并设置 `productionDeployable: true`，重新生成绑定当前源码哈希的清单，再按备份、校验、上传、`nginx -t`、reload 和 `curl` 验收流程执行。禁止使用 r1、rollback-real-data 文件或历史 zip 作为发布源。

## 2026-08-27 完成度审计与剩余任务

| 项目 | 当前证据 | 状态 |
|---|---|---|
| 生产代码、迁移、实时服务与白名单清理 | 215 项测试、本机构建、公网 health/REST/WSS、生产库查询 | completed |
| 商城隔离 | 前端开关关闭、商城迁移未部署、后台维护默认关闭 | completed |
| 独立测试任务 | 已建立“蛋蛋校园线上轮询测试”；因该任务未产出执行记录，本轮由主任务按同一隔离边界生成标准报告 | completed_with_main_session_fallback |
| 独立测试数据库与进程 | `dandan_campus_test`、`127.0.0.1:3311` 与 PM2 `dandan-world-test` 已建立；仅执行非商城迁移 | completed |
| 完整业务循环验收 | 隔离库真实写入 E2E 22/22 通过，覆盖注册、会话、资料、邀请码、任务、打听、盲盒、排行榜、WebSocket 与 REST 兜底 | completed |
| 文档最终校正 | 实施计划、测试报告、进度文件与 Obsidian 已同步本轮证据 | completed |

### 本轮完成顺序

1. 已建立 `dandan_campus_test`，测试服务只监听 `127.0.0.1:3311`，`.env.test` 权限为 600。
2. 已只向测试库执行非商城迁移，PM2 `dandan-world-test` 保持独立运行。
3. 已通过 SSH 隧道 `127.0.0.1:13311` 执行完整真实写入 E2E，报告为 `docs/online-test-reports/2026-08-27-isolated-e2e.md`。
4. 已完成 42 个测试文件、215 项测试及本地 TypeScript 构建。
5. 最新一轮按 SHA-256 差异仅上传 `dist/src/app.js` 与 `growth-school.html`，完成生产 health、REST、管理员登录、Cookie refresh、WSS、Nginx 和 PM2 日志验收。

## 目标

建立可用于生产演进的 CVM 自托管 MySQL 数据层，先安全保存用户数据，再逐步接入任务、蛋蛋币、评价、通知和管理后台。前端只通过后端接口访问数据库，不允许浏览器直连数据库。

## 方案假设

- 数据库：南京 CVM 上自托管 MySQL 8.0，InnoDB，UTF8MB4。
- 访问方式：后端服务和数据库在同一台 CVM，使用 `127.0.0.1:3306`。
- 认证：后端使用密码哈希、会话/JWT 和服务端权限校验。
- 一期范围：用户注册/登录、用户资料、五维统计、角色解锁、蛋蛋币账户及流水。
- 二期范围：任务、认领配对、提交验收、评价、通知、反馈和自动退款。

## 阶段

| 阶段 | 内容 | 状态 |
|---|---|---|
| 0 | 需求冻结与领域规则确认 | completed |
| 1 | 腾讯云资源、网络和安全配置 | dd_subpath_nginx_and_https_proxy_completed_sms_pending |
| 2 | 数据库 Schema、迁移和种子数据 | completed |
| 3 | 用户数据 API 与认证 | completed_cvm_dd_smoke_passed |
| 4 | 前端数据层接入与兼容迁移 | completed_dd_static_deployed_browser_verified |
| 5 | 盲盒交友模块与用户认证接入 | completed_cvm_and_dd_deployed |
| 6 | 任务、蛋蛋币、评价和通知闭环 | completed_local |
| 7 | 测试、备份、监控和上线验收 | production_verification_complete |

## 当前下一步

此前已上线版本的生产验收证据仍保留在历史记录中；本轮 r2 仅完成隔离测试与发布边界整改，尚未获生产批准、未上传生产、未 reload Nginx。后续只有在负责人明确批准并生成 production-approved 清单后，才能执行生产发布；商城继续由 `data-shop-enabled="false"` 与 `SHOP_ENABLED=false` 双重关闭，“航线”根站和 Supabase 配置继续保持隔离。

## 2026-08-27 后续审计状态

- [x] 第二轮任务中心审计：确认配对、直接取消、通知和公开任务发布者均已切换为服务端事实来源。
- [x] 本轮增量生产发布与健康/鉴权/日志验收完成。
- [ ] 后续继续按模块审计剩余历史前端兼容函数；仅在确认真实入口可达且会影响用户数据时新增失败回归测试并最小化修复。
- [ ] 每次后续发布继续保留商城双重关闭与非商城迁移边界，禁止运行全量 Prisma migration deploy。

## 2026-08-27 审计补充：前端唯一事实来源与协商取消

- [x] 角色/MBTI 选择改为先写入 API，再重新获取用户与角色状态。
- [x] 打听点赞、回答、评论与采纳成功后统一重新读取 REST 数据，不再把成功结果写入本地帖子对象。
- [x] 教学任务协商取消改为 MySQL 持久化流程：创建申请、对方同意或拒绝、接受时在同一事务取消任务并退回冻结蛋蛋币。
- [x] 只向生产库应用 `202608270002_task_cancellation_requests`，确认商城迁移 `202608260007_egg_mall` 仍未执行。
- [x] 本机 43 个测试文件、220 项测试，TypeScript 构建、Prisma schema、前端内联脚本解析均通过；隔离 E2E 23/23 通过。
- [x] 生产按哈希增量发布后端构建与两个前端文件；health、管理员登录、新接口、WSS、PM2 和 Nginx 复验通过。

## 2026-08-27 前端演示数据清理收口

- [x] 任务卡、任务详情、角色资料、MBTI、盲盒结果、问答和通知不再生成或保留本地演示数据。
- [x] 所有涉及用户可见业务状态的写入后均以 REST 回读为准；前端不再从昵称推断管理员、稀有度、等级、头像或固定统计值。
- [x] 真实性回归契约与全量测试通过（43 个文件、233 项），本机 TypeScript 构建和脚本语法检查通过。
- [x] 已按增量部署流程发布两个前端文件并完成公网哈希、health、鉴权、Nginx、PM2 和当前进程日志验收；服务器未执行 build，未运行商城迁移。

## 2026-08-27 运行时残留清理与任务完成链路

- [x] 清除页面初始化已删除函数调用和无后端持久化的邀请/组队完成分支。
- [x] 发布者确认提交改为调用真实完成接口并回读状态、流水，再进入评价流程；审核列表从服务端认领记录读取。
- [x] 237 项全量测试、构建与页面脚本解析通过；主页面已完成增量发布和公网验收。

## 2026-08-26 收口状态更新

- [x] 盲盒收件箱/会话 HTTP 轮询已接入，并在关闭窗口时清理定时器。
- [x] 本地回归：53 个测试通过、TypeScript 构建通过、Prisma schema 校验通过、前端脚本和内联脚本语法通过。
- [x] 390x844、768x1024、1440x900 三端无横向溢出，盲盒直达页仅输出内容组件。
- [x] 前端 r8 与后端 r7 压缩包已生成并完成 SHA-256、文件数量和禁入文件检查。
- [ ] 恢复可验证 CVM 会话后，按备份 -> 迁移 -> 构建 -> PM2 日志 -> `/health` -> 三端线上 smoke 执行生产发布。

## 2026-08-26 四项 Bug 修复

- [x] 注册表单和登录兼容策略
- [x] 任务每日发布上限与递减经验结算
- [x] 新用户静态任务隔离
- [x] 真实用户排行榜与八卦榜聚合
- [x] 测试、构建、Prisma 生成和文档记录
- [ ] CVM 生产发布与线上 smoke（等待 MFA）

## 验收总标准

- 用户注册、登录、退出和资料更新可持久化。
- 余额、冻结余额和流水在事务中保持一致，重复请求不会重复扣款。
- 任务状态只能通过合法状态转移改变。
- 关键权限、隐私字段和管理员操作由后端校验。
- 备份恢复、监控告警和回滚步骤经过演练。

## 已完成的本地搭建

- `server/` Node.js + TypeScript + Fastify 后端工程
- Prisma MySQL schema 和初始用户数据迁移
- 用户、会话、五维统计、角色、蛋蛋币账户、蛋蛋币流水、审计日志模型
- 注册、登录、退出、个人资料、角色、蛋蛋币账户和流水 API
- 输入校验、密码哈希、JWT 会话、公开资料脱敏
- 单元测试、健康检查测试和构建脚本
- 本地认证回归：6 个测试文件、19 个测试通过；TypeScript 构建通过
- 可重复的 CVM 构建、Prisma 迁移和健康检查脚本 `server/cvm-deploy.sh`
- `https://dsxnb.com/dd/` 子目录部署：静态文件 `/var/www/dd`，API `/dd/api` 代理到 `127.0.0.1:3310`
- `https://dsxnb.com/dd/health` 作为公开 API 健康检查入口，代理到 `127.0.0.1:3310/health`
- `https://dsxnb.com/dd/blind-box/` 盲盒交友静态模块，使用现有蛋蛋校园登录会话和 `dandan_world` 独立表
- 盲盒 API 线上验证：未登录 `GET /dd/api/buddy-box/recommendations` 返回 `401`；`/dd/health` 返回 `200`
- PM2 `dandan-world` 已修复环境变量注入并保存进程清单，避免重启后因缺少 `DATABASE_URL`/`JWT_SECRET` 退出
- `server/deploy/backup-dandan-world.sh`、`server/deploy/verify-backup.sh` 和 `docs/production-operations.md` 已加入；远程实际备份尚待 MFA 会话恢复后执行并验证
- 已将验证通过的 `index.html`、`app.js` 和 `buddy-box-api.js` 同步回原始 `D:\桌面文件\盲盒交友模块`，后续从该模块继续开发不会丢失认证/API 接入
- CVM 新版本目录 `/root/dandan-world-server-20260823220500`，旧版本目录保留用于回滚
- 公开子目录认证烟测输出 `AUTH_SMOKE_OK`

## 2026-08-26 本轮收口状态

- 本地完成：真实蛋蛋币流水查询、打听专属列表、回答采纳事务结算与通知、盲盒错误提示、学校/地区/任务联系方式敏感词校验。
- 本地验证完成：后端 47 项测试、TypeScript 构建、前端三个脚本和主站内联脚本语法、浏览器访客首屏与盲盒纯内容 smoke。
- 交付包已更新为 `output/releases/*20260826-business-iteration-r2.zip`，已做 SHA-256 和禁入文件边界检查。
- 生产状态保持待执行：备份 -> Prisma migrate deploy -> 构建 -> PM2 日志 -> `/health` -> 三端线上 smoke；未获得可验证 CVM shell 前不得宣称上线。

## 2026-08-26 本轮增量任务

| 项目 | 状态 | 说明 |
|---|---|---|
| 今日盲盒行动参与推荐排序 | completed_local | 推荐 API 接收可选 `action`，按 `todayActions` 同值/包含关系提升命中用户；抽盒揭示后前端刷新推荐 |
| 自动化契约 | completed_local | 后端排序契约、前端连接点契约均已先红后绿 |
| 生产发布 | pending_cvm | 仍需恢复可验证 CVM 会话后按备份、迁移核对、构建、PM2 日志、health 和三端 smoke 执行 |

## 2026-08-26（敏感词与盲盒本地开发收口）

- [x] 对盲盒高级玩法嵌套 JSON 接入服务端递归敏感词校验。
- [x] 对注册、资料、反馈字段补齐服务端敏感词校验；用户可见提示不展示具体敏感词。
- [x] 修正盲盒本地 API origin，并去除高级玩法无记录时的伪造数据兜底。
- [x] 完成本地后端 55 项测试、构建、Prisma、脚本语法、差异和三视口组件结构检查。
- [ ] CVM 会话恢复后先备份生产数据库，再逐条执行迁移、构建、PM2 日志、`/health` 和三端公网 smoke；未完成前不得标记上线。

## 2026-08-26（盲盒真实结果与交付）

- [x] 移除雷达和破冰助手的固定服务端结果伪造，改为真实空状态。
- [x] 好友申请事务补充双方用户行锁，保留既有关系状态与拒绝冷却规则。
- [x] 重新完成构建、Prisma、55 项测试、前端语法、差异和三端组件结构验证。
- [x] 已生成并核验前端 r10、后端 r9 交付包：18/46 文件，禁入文件 0，SHA-256 已写入验证记录。
- [ ] 恢复可验证 CVM 会话后，按备份 -> 迁移 -> 构建 -> PM2 日志 -> `/health` -> 公网 smoke 发布。

## 2026-08-26（我的打听与真实发布数据收口）

- [x] `/api/inquiries/mine` 增加当前用户专属的回复总数和最新回复摘要，保留原字段且不改表。
- [x] 打听页“我的打听”面板改用服务端专属汇总，不再从全站数据模拟消息汇总。
- [x] 普通用户发布任务取消开发示例文本兜底，空标题或描述不发起写库请求。
- [x] 已完成 58 项全量测试和 TypeScript 构建。
- [x] 已生成并核验前端 r11、后端 r10 包：18/46 文件、禁入项 0，SHA-256 已写入验证记录。
- [ ] CVM 恢复可验证会话后，严格按备份 -> 迁移状态核对 -> 构建 -> PM2 日志 -> `/health` -> 三端公网 smoke 发布。

## 2026-08-26（关机恢复后的工具链修复）

- [x] 恢复 Codex 本地执行宿主并确认命令执行可用。
- [x] 修复 agent-browser Windows 冷启动配置，专用 Chrome 自动启动、连接、快照和截图均已验证。
- [x] 清理不完整的 `animation-systems.partial-install` 遗留目录。
- [x] 新增 `docs/skills-healthcheck.ps1`，验证 74 个 Codex skill、57 个 Agents skill、5 个官方插件 manifest 和 agent-browser 配置。
- [x] 工具链验收输出 `SKILLS_HEALTH_OK`；生产发布状态保持未上线，避免把工具恢复误报为业务发布完成。

## 2026-08-27 管理员任务发布输入收口

- [x] 管理员发布任务时，描述字段改为必填；空描述不会再写入固定开发示例文本。
- [x] 本机全量回归：43 个测试文件、226 项测试通过；主页面内联脚本解析通过。
- [x] 仅增量发布 `backend-handoff-package/growth-school.html`，未构建服务器、未修改数据库、未触碰商城开关。
- [x] 生产 health 为 200，匿名任务查询按预期返回 401，Nginx active，`dandan-world` online 且错误日志为空。

## 2026-08-27 反馈前端可达性收口

- [x] 识别并修复反馈可见控件未绑定处理函数的问题，覆盖用户提交、附件、历史、管理员筛选与回复。
- [x] 新增失败回归契约后实现，保持反馈的用户归属、管理员权限与服务端状态转移规则不变。
- [x] 本地 43 文件、227 项测试、TypeScript 构建和脚本解析通过；仅更新生产静态页面并完成 health、鉴权、Nginx、PM2 日志验收。

## 2026-08-27 前端任务旧分支清理

- [x] 删除不可达的 DOM 本地造任务/删任务函数，避免生产页面生成固定演示数据。
- [x] 修复真实性契约的 CRLF/LF 边界匹配。
- [x] 本机全量回归 43 个文件、228 项通过；TypeScript 构建通过。
- [x] 备份后增量发布 `growth-school.html`；生产 health 200、匿名任务接口 401、Nginx active、PM2 online，当前进程无新 5xx。
- [x] 商城继续双重关闭；未执行商城迁移，未修改服务器环境或数据库。

## 2026-08-27 个人评价服务端化与生产发布

- [x] 个人页收到的评价改为调用 `GET /api/users/me/ratings`，后端按当前用户 ID 查询并聚合平均分与数量。
- [x] 清理任务完成、邀请通知和初始化中的剩余本地伪状态路径，保留既有 UI 与 REST 接口边界。
- [x] 完成本机 43 个测试文件、239 项测试、TypeScript 构建、前端脚本解析和差异检查。
- [x] 备份后只增量发布三个变化文件；服务器保留 `.env` 与 `node_modules`，不 build、不迁移数据库、不启用商城。
- [x] 完成远端正式文件与公网文件哈希、health、匿名鉴权、Nginx、PM2 和当前 PID 日志验收；生产 PID 为 `2506329`。
- [ ] 已认证评价接口的生产 200 烟测未执行：现有自动化脚本仅面向隔离测试库，避免在命令或报告中暴露生产密码、Cookie 或 JWT；本地授权接口契约已纳入 239 项回归。

## 2026-08-27 全站真实性完成度第三轮审计

- [x] 静态检索主站、API 客户端、盲盒和实时客户端中仍可达的硬编码业务数据、本地成功状态、随机结果和刷新后丢失路径。
- [x] 修复三个任务广场静态非零计数；用户名片改为按稳定 ID 动态读取公开资料并编码用户内容，两项均先完成失败契约再实现。
- [x] 继续复核动态内容输出编码和真实入口可达性；删除九个无入口旧任务函数，保留正常空状态、UI 常量和商城代码。
- [x] 运行定向契约、完整 244 项回归、TypeScript 构建、前端脚本解析与差异检查。
- [x] 按哈希仅增量发布后端 `app.js` 与主页面，并复验 health、鉴权、Nginx、PM2 与新 PID 日志。
- [x] 将结论同步到 `findings.md`、`progress.md`、Obsidian 变更日志和线上测试报告。

### 第三轮审计收口（2026-08-27 07:26 CST）

- [x] 三个任务广场初始计数归零，并以 REST 返回记录动态重算。
- [x] 用户名片按稳定用户 ID 读取公开资料；缺失资料显示空状态，用户内容经过输出编码。
- [x] 任务认领接口返回真实蛋蛋角色与稀有度，前端不再写死“生活互助 / N”。
- [x] 删除九个只有定义、没有入口的 DOM 任务刷新、详情和取消响应兼容函数，现行任务流程继续使用 REST 写入后回读。
- [x] TDD 专项契约 41/41；完整回归 43 个文件、244/244；TypeScript 构建、三份前端脚本和两段主页面内联脚本解析通过。
- [x] 仅按哈希增量发布 `dist/src/app.js` 与 `growth-school.html`；未上传无变化文件，未执行服务器 build、依赖安装、数据库迁移或环境变量修改。
- [x] 生产主页面哈希、health 200、匿名任务 401、Nginx、PM2 新 PID 与错误日志均已验收。

## 2026-08-27 原始需求总完成度审计

- [x] 确认独立任务“蛋蛋校园生产回归测试”仍由 10 分钟心跳持续运行，当前正在执行第 31 轮。
- [x] 补齐最新报告中未执行的隔离注册/邀请码、登录持久化、任务、打听、盲盒与认证 WSS 全链路：当前轮 23/23 通过。
- [x] 核对远端隔离 `.env.test` 和测试服务可用性，未读取或输出任何环境值；启动器改为进程内生成临时管理员凭据。
- [x] 补齐 `server/.env.test.example` 无秘密配置模板，并由隔离守卫限制为 `dandan_campus_test`。
- [x] 对原始 11 项交付清单完成当前证据矩阵；生产健康、完整隔离 E2E、全量单元测试和构建均有当轮证据。

### 本轮错误记录

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| `Select-String` 同时读取不存在的 `server/.env.test.example`，导致该批检索退出 1 | 1 | 将可选文件存在性检查与脚本检索拆开；确认缺失后按 TDD/配置边界补模板，不重复原命令 |
| 远端 `awk` 中的 `$1` 被本机 PowerShell 展开，两个变量名检查命令语法错误 | 1 | 改用不含 `$1` 的 `grep + cut`；确认 `.env.test` 与 PM2 环境均无 E2E 管理员变量，未读取或输出任何值 |
| 未携带站点 `Host` 头向 `127.0.0.1/dd/health` 发起只读检查，命中默认 Nginx 虚拟主机并返回 404 | 1 | 改用 `https://dsxnb.com/dd/health` 公开地址，返回 200；后续服务器本地探测必须指定站点 Host |

## 2026-08-27 隔离 E2E 启动器验收

- [x] 新增隔离管理员守卫，仅允许 `dandan_campus_test`、固定隔离账号与长度不少于 16 位的临时密码。
- [x] 新增本机启动器：指定磁盘私钥、仅本机 SSH 隧道、临时环境变量，并在 finally 中清理凭据和隧道。
- [x] 仅向测试目录增量上传守卫构建文件与管理员引导脚本；未改生产代码、环境、依赖、进程或数据库迁移。
- [x] 当前轮隔离真实写入 E2E 23/23 通过，报告为 `docs/online-test-reports/2026-08-27-isolated-e2e.md`。
- [x] 已向十分钟持续回归任务发送启动器使用方式，后续轮次不再依赖人工注入凭据。

## 2026-08-27 认证资料、任务广场与部署后三轮检测

- [x] 修复登录状态与个人页身份不一致，角色区改为真实数据角色护照。
- [x] 修复用户名密码注册、可选邮箱无验证码及中文错误反馈。
- [x] 完成任务广场彩色卡片、手机适配、零消息数字隐藏但入口保留。
- [x] 补齐打听模块空缓存和筛选状态初始化，消除首屏 `ReferenceError`。
- [x] 完成本机 281 项回归、构建、语法、差异和桌面/手机视觉验证。
- [x] 增量部署到 `https://dsxnb.com/dd/`，未执行商城迁移、未触碰“航线”或 Supabase。
- [x] 完成三轮独立检测，每轮 23/23，合计 69/69；生成三份逐项报告与一份合并报告。
- [x] 本机完整回归 `44` 个测试文件、`249/249` 通过，TypeScript 构建成功；持续任务首轮也已按启动器独立完成 `23/23`。
