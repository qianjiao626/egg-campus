# 蛋蛋校园部署后三轮检测报告

- 生产地址：`https://dsxnb.com/dd/`
- 隔离写入环境：`dandan_campus_test`，通过 SSH 隧道访问 `127.0.0.1:3311`
- 检测方式：每轮生成全新临时凭据和 `[demo]` 测试记录；生产数据库仅做 health 与运行状态只读检查
- 总结果：三轮均为 `23/23`，合计 `69/69`；三轮生产 health 均为 HTTP 200

| 测试时间 | 用例项 | 预期结果 | 实际结果 | 是否通过 | 问题简述 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-27T04:03:19.172Z | 第 1 轮完整业务 E2E 与生产健康检查 | 23 项业务链路全部通过；生产 health=200 | 隔离 E2E 23/23；`/dd/health` 返回 `200 {"status":"ok"}` | 通过 | 无 |
| 2026-08-27T04:05:01.902Z | 第 2 轮完整业务 E2E 与生产健康检查 | 使用全新会话重复 23 项业务链路；生产 health=200 | 隔离 E2E 23/23；`/dd/health` 返回 `200 {"status":"ok"}` | 通过 | 无 |
| 2026-08-27T04:06:36.322Z | 第 3 轮完整业务 E2E 与生产健康检查 | 使用全新会话重复 23 项业务链路；生产 health=200 | 隔离 E2E 23/23；`/dd/health` 返回 `200 {"status":"ok"}` | 通过 | 无 |
| 2026-08-27T04:07:00Z | 生产运行状态收口 | PM2 online、Nginx active、错误日志为空、新静态资源可访问 | `dandan-world` online；Nginx active；生产与隔离 PM2 error log 均为 0 bytes；Anime 资源 HTTP 200 | 通过 | 无 |

三轮均覆盖：健康检查、管理员登录与权限、用户名密码注册、邀请码绑定与奖励、Cookie 刷新和登录恢复、个人资料首次及二次编辑、任务发布/审核/修改/公开可见/接取/放弃/协商取消、打听发布/回复/点赞/采纳、盲盒偏好/抽取/推荐、好友请求去重与聊天权限、私有 WebSocket 事件、排行榜动态数据、WebSocket 断线后的 REST 兜底。

详细逐项记录：

- `2026-08-27-production-three-rounds-round-1.md`
- `2026-08-27-production-three-rounds-round-2.md`
- `2026-08-27-production-three-rounds-round-3.md`

视觉与前端状态补充：部署前已用 Playwright 在 1280x900 与 375x812 核对任务广场；角色护照使用同一认证用户状态显示昵称和学校；五个零消息数字均隐藏且入口容器保留。生产访客首屏未发现 JavaScript `ReferenceError`，未登录请求返回预期 401。
