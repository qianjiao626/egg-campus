# 蛋蛋校园线上只读冒烟测试报告

- 测试时间：2026-08-27（Asia/Shanghai，首轮）
- 测试环境：生产线上只读冒烟，`https://dsxnb.com/dd/`
- 数据库：未连接、未写入；未创建账号
- 测试轮次：01

| 用例项 | 预期结果 | 实际结果 | 是否通过 | 问题简述 |
|---|---|---|---|---|
| 主页 `/dd/` | HTTP 200，返回 HTML | HTTP 200，`text/html`，约 724 KB | 通过 | 首次响应约 5.6 s，未见 5xx |
| 健康接口 `/dd/health` | HTTP 200，JSON 健康状态 | HTTP 200，`application/json`，15 bytes | 通过 | 无 |
| 公开排行榜 `/dd/api/users/leaderboard` | 公开接口可访问且无 5xx | HTTP 200，返回 JSON，约 4.3 KB | 通过 | 无 |
| 登录页 `/dd/login` | 页面可加载且无 5xx | HTTP 200，返回 HTML | 通过 | 本轮未提交登录 POST，避免副作用 |
| 登录接口 GET `/dd/api/auth/login` | 不应出现服务器 5xx | HTTP 404 | 通过 | GET 不匹配 POST 路由，非服务器错误 |
| 注册接口 GET `/dd/api/auth/register` | 不应出现服务器 5xx | HTTP 404 | 通过 | GET 不匹配 POST 路由，非服务器错误 |
| 未授权任务接口 `/dd/api/tasks/mine` | 未登录应拒绝访问，不应 5xx | HTTP 401 | 通过 | 符合鉴权预期 |
| 未授权树洞接口 `/dd/api/inquiries` | 未登录应拒绝访问，不应 5xx | HTTP 401 | 通过 | 符合鉴权预期 |
| 未授权盲盒接口 `/dd/api/buddy-box/features` | 未登录应拒绝访问，不应 5xx | HTTP 401 | 通过 | 符合鉴权预期 |
| 静态脚本 `api-client.js` | HTTP 200，JavaScript 可加载 | HTTP 200，约 26 KB | 通过 | 无 |
| 静态脚本 `sensitive-filter.js` | HTTP 200，JavaScript 可加载 | HTTP 200，约 834 KB | 通过 | 文件较大，但无 5xx |
| 静态图片 `char-eggy-game.jpg` / `char-eggy-hermit.jpg` | HTTP 200，图片可加载 | 均 HTTP 200，`image/jpeg` | 通过 | 无 |
| 盲盒静态页 `/dd/blind-box/` | HTTP 200，页面可加载 | HTTP 200，`text/html` | 通过 | 无 |

## 结论

本轮只读线上冒烟通过，检查项未发现 HTTP 5xx。测试数据库 `dandan_campus_test` 尚未配置，因此未执行注册、登录持久化、任务、盲盒、树洞、排行榜写入场景、邀请码或 WebSocket 回归。
