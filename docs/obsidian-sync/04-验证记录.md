---
title: 验证记录
type: verification
updated: 2026-08-26
tags:
  - verification
  - evidence
---

# 验证记录

## 2026-08-24 本地验证

| 检查 | 结果 | 证据 |
|---|---|---|
| `npm test --prefix server` | 通过：8 个测试文件、30 个测试 | 当前工作区命令输出 |
| `npm run build --prefix server` | 通过 | 当前工作区命令输出 |
| `node --check backend-handoff-package/blind-box/app.js` | 通过 | 当前工作区命令输出 |
| `node --check backend-handoff-package/blind-box/buddy-box-api.js` | 通过 | 当前工作区命令输出 |
| 盲盒 feature 写入幂等契约 | 通过 | `server/tests/buddy-box-contract.test.ts` |
| 前端认证契约 | 通过 | `server/tests/frontend-auth-contract.test.ts` |
| 认证验证码规则 | 通过 | `server/tests/verification.test.ts`、`validation.test.ts` |
| 注册 -> refresh -> `/api/users/me` -> logout -> 旧 refresh 失效 | 通过 | `server/tests/auth-session-flow.test.ts`，真实 Fastify 路由和 Cookie 头链路（数据库 mock） |

## 2026-08-24 当前工作区复验

| 检查 | 结果 | 证据 |
|---|---|---|
| `npm test --prefix server` | 通过：10 个测试文件、36 个测试 | 2026-08-24 当前工作区命令输出 |
| `npm run build --prefix server` | 通过 | 当前工作区命令输出 |
| 盲盒三个脚本 `node --check` | 通过 | 当前工作区命令输出 |
| `https://dsxnb.com/dd/health` | HTTP 200 | 本轮公开 HTTP 检查 |
| `https://dsxnb.com/dd/blind-box/` | HTTP 200，页面标题为内容组件 | 本轮公开 HTTP 检查 |
| 未登录盲盒推荐接口 | HTTP 401 | 本轮公开 HTTP 检查 |
| Cookie Path 覆盖盲盒 API | 通过：上游 `/api` -> 公开 `/dd/api` | `server/tests/auth-session-flow.test.ts`、`server/deploy/nginx-dsxnb-dd.conf` |

## 2026-08-24 认证与盲盒持久化收口复验

| 检查 | 结果 | 证据 |
|---|---|---|
| 仅昵称 + 自设密码注册 | 通过：不填写邮箱/手机号时无需验证码，仍签发 HttpOnly refresh Cookie | `server/tests/auth-session-flow.test.ts` 路由测试 |
| 带联系方式注册规则 | 通过：填写邮箱或手机号时仍要求已验证 token | `server/src/app.ts` 注册路由、`server/tests/verification.test.ts` |
| 盲盒高级玩法用户归属 | 通过：写入记录带当前 JWT 用户 ID，重复幂等键不会重复写入 | `server/tests/buddy-box-contract.test.ts`、`server/src/app.ts` |
| 盲盒全部 adapter | 通过：33 个 adapter 均经 `/api/buddy-box/features` 或用户 API 访问 | `backend-handoff-package/blind-box/buddy-box-api.js` |
| 盲盒刷新后恢复留言与玩法状态 | 通过：新增用户记录列表接口，前端启动时恢复 board 和 feature 状态 | `server/tests/buddy-box-contract.test.ts`、`backend-handoff-package/blind-box/app.js` |
| 管理员会话刷新后保留角色 | 通过：refresh 重新从服务端用户记录签发 `role=admin` 的 access token | `server/tests/auth-contract.test.ts` |

本地最新测试应以当前命令输出为准；线上真实账号跨关闭/重开浏览器、管理员角色和生产迁移仍需 CVM 发布后验收。

最新本地复验（补齐管理员角色恢复后）：`npm test --prefix server` 为 10 个测试文件、42 个测试通过；构建、Prisma 校验和盲盒脚本语法检查均通过。

## 2026-08-24 CVM 只读部署检查

- OrcaTerm 会话已恢复，CVM 上 `dandan-world` PM2 进程显示 `online`，当前目录仍为 `/root/dandan-world-server-20260824004500`。
- `/dd/health` 公开检查返回 HTTP 200。
- `/var/www/dd/growth-school.html`、`blind-box/index.html` 和 `blind-box/buddy-box-api.js` 的线上哈希与当前工作区最新包不一致，说明新认证持久化包尚未发布。
- 本次只执行只读检查，没有上传文件、重启进程、执行迁移，也没有读取或代填任何密码、Token、验证码或 MFA。

> 本轮还检查了线上 `growth-school.html` 的静态内容，仍能看到旧版绝对 iframe 路径 `/dd/blind-box/`；它可正常指向同一线上盲盒内容页，但尚未替换为当前工作区最新前端包。线上静态文件更新需要 CVM/文件发布权限。

本轮公开线上只读 smoke：`/dd/health`、`/dd/`、`/dd/blind-box/` 均返回 HTTP 200；未携带会话访问 `/dd/api/auth/refresh` 和 `/dd/api/buddy-box/recommendations` 均返回 HTTP 401。真实注册、Cookie 跨刷新、数据库迁移和盲盒跨用户隔离仍未在生产账号上验收。

本轮尝试通过腾讯云 OrcaTerm 连接 CVM；终端连接后触发子账号 MFA 身份验证，当前需要账号持有人在浏览器窗口输入动态码。未代填 MFA，也未读取或记录任何验证码。

> 本轮公开 refresh 请求在无 Cookie 时返回 401，不能从匿名响应证明生产 Cookie Path；必须在发布后用真实登录响应检查 `Set-Cookie: Path=/dd/api`。

## 2026-08-24（授权发布核对）

- 用户已明确授权上传、迁移和重启。
- 授权后通过 OrcaTerm 尝试重新传输和切换，但公开缓存绕过后的 `blind-box/buddy-box-api.js` 哈希仍为旧值，不能证明前端同步成功。
- `/dd/health`、`/dd/`、`/dd/blind-box/` 仍返回 200，匿名推荐接口仍返回 401；这些只能证明现网版本可用，不能证明 v5 已发布。
- 因远端终端输出/文件落盘证据不足，本轮未将线上版本标记为完成，也未删除旧目录或旧静态备份。

## 2026-08-25（发布继续）

- 重新连接 OrcaTerm 后确认腾讯云 MFA 仍待账号持有人完成，当前显示 `0 个会话已连接`。
- 后端 v5 部署包远端 SHA-256 已核对一致；前端大包传输曾因终端节流产生不一致，未解压、未同步、未切换。
- 线上仍只确认旧版本公开 smoke：健康页 200、主站 200、盲盒页 200、匿名推荐 401。
- 当前唯一待用户操作：在腾讯云 MFA 窗口完成动态码验证。不得代填或记录动态码。

## 已知线上证据

以下结论来自此前部署验收记录，最新后端版本或迁移重新发布后需再次复核：

- `https://dsxnb.com/dd/` 返回 200。
- `https://dsxnb.com/dd/blind-box/` 返回 200，页面只输出盲盒内容组件。
- `https://dsxnb.com/dd/health` 返回 200 和健康状态。
- 未登录请求 `/dd/api/buddy-box/recommendations` 返回 401，说明受保护路由没有匿名放行。
- 根站 `https://dsxnb.com/` 仍保持“航线”站点标题和配置。
- 认证核心 CVM 烟测曾输出 `AUTH_SMOKE_OK`；找回密码扩展流程在最新构建重启后仍需重新执行。

## 缺失的强证据

以下项目不能仅以本地测试代替线上验收：

1. 真实注册后关闭浏览器、重新打开主站，Cookie 恢复 `/api/users/me`。
2. 普通用户与管理员分别登录，确认刷新后角色和授权均保留。
3. 主站与盲盒 iframe 共用 Cookie，盲盒偏好和玩法记录在刷新后仍可读取。
4. `BuddyFeatureRecord` 迁移已在生产库执行，写入、读取、公开 scope 和幂等重复请求均通过线上 smoke。
5. 退出登录和重置密码后旧会话确实失效。
6. 腾讯云 SMS 真机发送、频率限制和失败提示。
7. 备份文件恢复到独立库并能通过健康检查。

## 验证原则

“代码存在”与“线上可用”分开记录；只有命令输出、HTTP 状态、数据库迁移状态或浏览器行为可以将待办改为完成。任何包含凭据或验证码的日志只记录脱敏结论，不复制原文。

## 2026-08-25（v5 线上发布完成）

- MFA 已由账号持有人完成，OrcaTerm 恢复为 `1 个会话已连接`。
- 后端 v5 SHA-256：`25b6bff69afc33cd0462921e0acdc453ccbab2ded6d5379f849957fde60b7bc1`。
- 前端 v5 SHA-256：`70c2f005225abf53358e434413875ec3b8c1c3734a220f4998a4c1204344593c`；远端解码包哈希一致。
- 线上静态文件哈希一致：盲盒 API 与主站页面均与本地 v5 解包文件一致。
- `npx prisma migrate status`：4 migrations found，Database schema is up to date；包含 `BuddyFeatureRecord` 迁移。
- PM2 `dandan-world` 已切换到 `/root/dandan-world-server-20260824-auth-persistence-final-v5/dist/src/server.js`，状态 `online`，并已 `pm2 save`。
- 公开 smoke：`/dd/health` 200、`/dd/` 200、`/dd/blind-box/` 200、匿名推荐接口 401；根站 `https://dsxnb.com/` 200 且“航线”标题保持不变。
- 生产认证 smoke（随机测试账号，未记录凭据）：注册 201、refresh 200、`/users/me` 200、盲盒 features 200、带 Bearer 退出 200；注册响应 Cookie Path 为 `/dd/api`。

## 2026-08-25（联系方式可选版本生产复验）

| 检查 | 结果 | 证据 |
|---|---|---|
| 后端迁移 | 通过：4 个 migration，数据库无待执行迁移 | CVM `prisma migrate deploy` 输出 |
| PM2 应用日志 | 通过：无 MySQL 1045、未处理异常或启动失败 | `pm2 logs dandan-world --lines 50 --nostream` |
| `.env` 数据库配置结构 | 通过：`DATABASE_URL` 存在，末尾为换行，无尾随反斜杠 | CVM 脱敏结构检查 |
| 本机与公网健康检查 | HTTP 200 | `/health` 检查 |
| 匿名推荐接口 | HTTP 401，符合登录保护预期 | `/api/buddy-box/recommendations` 公开请求 |
| 页面入口 | `/dd/` 与 `/dd/blind-box/` 均 HTTP 200 | 发布后公开请求 |
| 仅昵称 + 密码认证 | 通过：注册、当前用户、登录、注销和旧 access token 失效 | 一次性生产 smoke，临时数据已清理 |
| 临时 smoke 数据清理 | 通过：用户剩余数为 0 | Prisma 精确查询；依赖记录先于用户删除 |
| 数据库备份 | 通过：gzip SQL 备份可读 | `/root/backups/dandan_world/dandan_world-20260825103836.sql.gz` |

发布包 SHA-256：后端 `28B22AF4993D264795DC906529F0FBF6FC85B033695517CEC7CF8B0E49D97395`，前端 `89884703C9951FC770F1376B51E7CD0549681D9295BC309AEF0567F51E320E`。本节不记录任何生产密码、数据库连接字符串、令牌或验证码。

## 2026-08-26（业务迭代本地验证）

- 新增 Prisma 迁移 `202608260001_business_iteration`：任务、反馈、打听、回复、通知表；好友关系更新时间与查询索引；新账户默认蛋蛋币 100。
- 后端新增任务发布/本人查询/管理员审核、反馈提交/本人历史/管理员处理、打听/回复/通知接口，并统一后端敏感词双重校验；提示不返回具体词语。
- 盲盒推荐过滤隐身用户并按 MBTI/兴趣相似度排序；消息和聊天历史要求双方已接受好友；好友重复请求、拒绝后 30 分钟冷却、待处理请求均有明确错误码；新增拒绝接口。
- 前端移除盲盒 `NEW` 角标，补充手机/平板响应式布局、访客首屏登录状态、任务/反馈真实 API 接入和统一敏感词提示。
- 验证通过：`npm test`（12 个测试文件、47 个测试）、`npm run build`、前端三个脚本 `node --check`。
- 尚未执行生产迁移、PM2 重启和线上 smoke；本次未读取或记录任何 CVM 密码、数据库连接字符串、Token、验证码或 MFA。
- 后续前端复核补齐：我的任务同步只保留当前用户 API 返回记录，新增待审核/已审核/已完成/待修改筛选，管理员通过/退回按钮调用真实审核接口。
- 迁移二次审阅已修正 Prisma 漂移：Feedback/Inquiry 字段长度、Task 复合索引、Notification JSON 非空、TencentDB 兼容的余额 `MODIFY COLUMN`。

## 2026-08-26（本地收口复验）

- `npm test`：12 个测试文件、47 个测试全部通过。
- `npm run build`：TypeScript 构建通过。
- `node --check`：`api-client.js`、`blind-box/app.js`、`blind-box/buddy-box-api.js` 通过；主站 HTML 内联脚本 3 段通过 `vm.Script` 抽取校验。
- `git diff --check`：无空白错误；仅有 Windows 换行提示。
- 记录的校验命令错误：直接对 `.html` 执行 `node --check` 会报 `ERR_UNKNOWN_FILE_EXTENSION`；PowerShell 传递正则字面量会报 `Unterminated regexp literal`，均已改用正确的抽取方式复验。
- 当前结论：本地代码可构建、测试通过；本轮生产迁移和线上 smoke 尚未执行，不能据此宣称已上线。
- 浏览器烟测：本地 `growth-school.html` 访客首屏显示登录界面；`/blind-box/` 标题为“盲盒交友内容组件”，快照只含盲盒业务内容；主站截图 `output/playwright/growth-desktop-current.png`。
- 交付包边界检查：前端 18 个文件、后端 44 个文件；未包含 `node_modules`、`dist`、`.env`、本地密码文件或暂存目录。

## 2026-08-26（任务结算本地复验）

- `npm test`：12 个测试文件、49 个测试全部通过，新增幂等认领和重复评价契约覆盖。
- `npm run build`：TypeScript 构建通过；`npx prisma validate` 在临时占位 `DATABASE_URL` 下通过。
- 前端 `api-client.js`、盲盒脚本和主站 3 段内联脚本语法检查通过；`git diff --check` 无空白错误。
- r3 交付包：前端 18 个文件，后端 45 个文件；均无 `.env`、本地密码、`node_modules` 或 `dist`。
- 前端包 SHA-256：`D8E44F8E008F17BAF2463DFF3B51559D3829969B4124D27F2347B7E754DBBFD1`。
- 后端包 SHA-256：`B70DD8EFF8805DFA989FEAFA287B9DFFF4F096892F4239913A6B0416F32C4D5D`。
- 生产迁移、备份、PM2 日志、健康检查和三端线上 smoke 仍待 CVM 会话恢复后执行。
- 本地 agent-browser smoke：主站标题和侧边栏可见；盲盒页标题为“盲盒交友内容组件”，快照无插件导航外壳；截图保存于 `output/playwright/growth-final-local.png` 与 `output/playwright/blind-box-final-local.png`。

## 2026-08-26（收口补丁与多端复验）

| 检查 | 结果 | 证据 |
|---|---|---|
| 首屏角色恢复 | 通过：`#stage` 在 `dandan:session-restored` 前隐藏，访客/管理员/普通用户渲染后显示 | `backend-handoff-package/growth-school.html` |
| 管理员反馈回复前置过滤 | 通过：发送请求前调用 `apiClient.validateUserText`，服务端仍做第二层校验 | `growth-school.html`、`server/src/app.ts` |
| DEMO 流水标记 | 通过：静态开发蛋蛋币/经验明细保留并显示 `DEMO`，真实流水不标记 | `growth-school.html` |
| 好友状态显示 | 通过：推荐接口返回关系状态；前端显示已是好友、已有待处理请求、对方已拒绝，拒绝冷却期禁用申请 | `server/src/app.ts`、`blind-box/app.js` |
| 关系记录幂等 | 通过：重新申请旧反向拒绝关系复用原记录，不新建反向重复行 | `server/src/app.ts` |
| 后端测试 | 通过：12 个测试文件、49 个测试 | `npm test` |
| TypeScript 构建 | 通过 | `npm run build` |
| Prisma schema | 通过 | `npx prisma validate`（临时占位 URL） |
| JavaScript 语法 | 通过 | `node --check` 三个脚本；主站内联脚本无正则抽取检查 |
| 手机视口 | 通过：390x844，无横向溢出 | `output/playwright/blind-box-mobile-r5.png` |
| 平板视口 | 通过：768x1024，无横向溢出 | `output/playwright/blind-box-tablet-r5.png` |
| 桌面视口 | 通过：1440x900，无横向溢出 | `output/playwright/blind-box-desktop-r5.png` |
| 前端交付包 | 通过：18 文件、无禁入文件 | `output/releases/dandan-frontend-dd-20260826-business-iteration-r5.zip` |
| 后端交付包 | 通过：44 文件、无禁入文件 | `output/releases/dandan-server-deploy-20260826-business-iteration-r4.zip` |

本轮浏览器使用当前包专用静态服务；由于未启动本地后端代理，日志中的 API 404 仅表示后端未运行，不作为业务失败证据。生产迁移、PM2 日志和公网三端 smoke 仍未执行。

## 2026-08-26（今日盲盒行动匹配）

| 检查 | 结果 | 证据 |
|---|---|---|
| 推荐 API action 参数 | 通过 | `server/tests/buddy-box-contract.test.ts`：同一行动用户优先返回 |
| 前端行动透传 | 通过 | `server/tests/blind-box-isolation-contract.test.ts`：`getRecommendations(action)` 与抽盒后刷新连接点 |
| 专项测试 | 通过 | 两个专项测试文件全部通过 |
| 数据库迁移 | 不需要 | 仅改变查询排序和前端请求参数，无 schema 变更 |
| 生产上线 | 待执行 | 尚未取得可验证 CVM 会话；不得以本地测试代替线上证据 |

交付包：前端 `output/releases/dandan-frontend-dd-20260826-business-iteration-r6.zip`（18 文件，SHA-256 `A10207C75EEC4CA06034424934869BA3DC701926B64D287B7CFED3E868A3D2D9`）；后端 `output/releases/dandan-server-deploy-20260826-business-iteration-r5.zip`（46 文件，SHA-256 `50508687A484861F24019A3937765F842C1A788DB8A3077296127D43302A43A8`）。两包均通过禁入文件边界检查。

## 2026-08-26（HTTP 轮询最终复验）

| 检查 | 结果 | 证据 |
|---|---|---|
| 后端测试 | 通过：12 个文件、53 个测试 | `server/npm test` |
| TypeScript 构建 | 通过 | `server/npm run build` |
| Prisma schema | 通过 | `npx prisma validate`，当前进程注入完整占位 URL |
| JavaScript 语法 | 通过 | 三个 `node --check`；主站 2 段内联脚本 `vm.Script` |
| 浏览器结构 | 通过 | 盲盒标题为“盲盒交友内容组件”，快照无插件导航外壳 |
| 移动端 | 通过：390x844，无横向溢出 | `output/playwright/blind-box-mobile.png` |
| 平板端 | 通过：768x1024，无横向溢出 | `output/playwright/blind-box-tablet.png` |
| 桌面端 | 通过：1440x900，无横向溢出 | `output/playwright/blind-box-desktop.png` |
| 轮询生命周期 | 通过 | 收件箱 15 秒、会话 5 秒，关闭时清理；契约测试与源码审计 |
| 交付包 | 通过：18/46 文件，无禁入项 | r7 前端、r6 后端压缩包及 SHA-256 |

## 2026-08-26（任务认领通知测试兼容修复）

| 检查 | 结果 | 证据 |
|---|---|---|
| 后端测试 | 通过：12 个文件、53 个测试 | `server/npm test` |
| 任务认领专项回归 | 通过：4 个测试 | `npx vitest run tests/business-contract.test.ts` |
| TypeScript 构建 | 通过 | `server/npm run build` |
| Prisma Client | 通过 | `server/npx prisma generate` |
| Prisma schema | 通过 | `DATABASE_URL` 注入完整占位 URL 后 `server/npx prisma validate` |
| 前端 JavaScript | 通过 | 三个 `node --check`；主站 2 段内联脚本 `vm.Script` |
| 浏览器结构与响应式 | 通过 | 盲盒纯内容组件；390/768/1440 视口无横向溢出；截图写入 `output/playwright/*-final.png` |
| 差异检查 | 通过 | `git diff --check`（仅换行提示，无空白错误） |

本次失败根因是测试未 mock 新增的任务认领通知写入，导致隔离测试尝试初始化真实 Prisma 客户端；已仅补充测试 mock，生产通知逻辑保持不变。生产 CVM 备份、迁移、PM2 日志和公网 smoke 仍未执行。

交付包（本轮重新生成）：前端 `output/releases/dandan-frontend-dd-20260826-business-iteration-r8.zip`，18 文件，SHA-256 `FEF8F2666465476D16C0A3117A909BA0F607FF0FDDAD75FC1D15D696E40E9E2F`；后端 `output/releases/dandan-server-deploy-20260826-business-iteration-r7.zip`，47 文件，SHA-256 `7FDB9C4CFD179AE59A0E5082CFD1D8D5835C10B2F9222FC572C7A6D1F5AEA637`。

本轮浏览器使用静态前端服务，API 404 仅表示未启动本地后端代理；生产备份、迁移、PM2 日志和公网 smoke 仍待 CVM 会话恢复。

## 2026-08-26（敏感词递归校验与盲盒组件复验）

| 检查 | 结果 | 证据 |
|---|---|---|
| 后端测试 | 通过：12 个文件、55 个测试 | `server/npm test` 退出码 0 |
| TypeScript 构建 | 通过 | `server/npm run build` 退出码 0 |
| Prisma Client / schema | 通过 | `npx prisma generate`、完整占位 `DATABASE_URL` 下 `npx prisma validate` |
| 前端脚本语法 | 通过 | `api-client.js`、`blind-box/app.js`、`blind-box/buddy-box-api.js` 的 `node --check` |
| 差异检查 | 通过 | `git diff --check` 无空白错误 |
| 敏感词回归 | 通过 | 嵌套 JSON 高级玩法 payload 被服务端拦截，提示不含命中词 |
| 盲盒直达组件 | 通过 | 无 header/nav/footer；390x844、768x1024、1440x900 无横向溢出、无 `New` |
| 主站嵌入 | 通过 | 点击 `[data-page=buddybox]` 后右侧 `#page-buddybox` 激活，iframe 同源内容无插件外壳 |
| 本地 API 连通 | 未启动 | 静态服务下 3310 未启动会出现连接拒绝；这不是生产可用性证据 |
| 生产发布 | 待执行 | 尚无可验证 CVM/MFA 会话，未执行备份、迁移、PM2 与公网 smoke |

交付包：前端 `output/releases/dandan-frontend-dd-20260826-business-iteration-r9.zip`（18 文件，SHA-256 `0FFF0CE68A49C5E024D82C8417B0F978A708478DD98BDB60501D434D1E699F0A`）；后端 `output/releases/dandan-server-deploy-20260826-business-iteration-r8.zip`（46 文件，SHA-256 `63A15736E15DDE1AECA51BFB1DC988D56546C885634C4EFB45F69A2B39226FDB`）。禁入项检查均为 0。

## 2026-08-26（盲盒真实结果与并发锁复验）

| 检查 | 结果 | 证据 |
|---|---|---|
| 后端测试 | 通过：12 个文件、55 个测试 | `server/npm test` 退出码 0 |
| TypeScript 构建 | 通过 | `server/npm run build` 退出码 0 |
| Prisma Client / schema | 通过 | `npx prisma generate`；完整占位 `DATABASE_URL` 下 `npx prisma validate` |
| 前端语法 | 通过 | 三个前端脚本的 `node --check` |
| 差异检查 | 通过 | `git diff --check` 无空白错误 |
| 直达组件外壳 | 通过 | DOM 无 `header`、`nav`、`footer` |
| 响应式 | 通过 | 390x844、768x1024、1440x900 的 `scrollWidth` 均等于视口宽度 |
| 主站嵌入结构 | 通过 | `#page-buddybox` 和同源 `blind-box/` iframe 存在 |
| 生产发布 | 待执行 | 本轮未执行 CVM 备份、迁移、PM2 日志、`/health` 或公网 smoke |

说明：本地静态预览没有启动 3310 API，控制台连接错误不作为业务失败或生产可用性证据。

交付包：前端 `output/releases/dandan-frontend-dd-20260826-business-iteration-r10.zip`（18 文件，SHA-256 `D33240081899937EC2544A746408990CE743B2C8FD60E7CF7F0B1CD6E55EA584`）；后端 `output/releases/dandan-server-deploy-20260826-business-iteration-r9.zip`（46 文件，SHA-256 `19A5BFA7F9E6BDF2504A23DE22F8CC468AC229FA041AE23E8E4C9464FA57EABF`）。两包禁入项检查均为 0。

## 2026-08-26（我的打听回复汇总与真实发布复验）

| 检查 | 结果 | 证据 |
|---|---|---|
| 我的打听归属 | 通过 | `GET /api/inquiries/mine` 查询条件固定为当前认证用户 |
| 回复汇总 | 通过 | 返回 `replyCount` 与 `recentReplies`，回复者与内容均来自 MySQL 关系查询 |
| 前端汇总渲染 | 通过 | 打听面板消费 `recentReplies`、`replyCount` 并显示“我的打听回复”摘要 |
| 空任务保护 | 通过 | 发布任务空标题或描述直接提示，不创建 API 请求 payload |
| 回归测试 | 通过：12 个文件、58 个测试 | `server/npm test` 退出码 0 |
| TypeScript 构建 | 通过 | `server/npm run build` 退出码 0 |
| 生产发布 | 待执行 | 本轮未进行 CVM 备份、迁移、PM2 日志、`/health` 或公网 smoke |

交付包：前端 `output/releases/dandan-frontend-dd-20260826-business-iteration-r11.zip`（18 文件，SHA-256 `FD51BB38499DF6531E93F9A46FA92AD9F80CBF1376E564B3165E4A5DA4700968`）；后端 `output/releases/dandan-server-deploy-20260826-business-iteration-r10.zip`（46 文件，SHA-256 `61916188C3E1FA9CD844F9F5A5D3AC6B54AA9099488F62F333404B6B284639AD`）。两包禁入项检查均为 0。

## 2026-08-26（公网只读状态）

| 检查 | 结果 | 证据 |
|---|---|---|
| 线上健康检查 | 当前服务存活 | `https://dsxnb.com/dd/health` 返回 `200 {"status":"ok"}` |
| 前端版本 | 尚未部署 r11 | 线上首页缺少 `myInquiriesPanel` 与 `recentReplies` |
| 旧发布兜底 | 仍在线上 | 线上源码仍包含旧任务标题示例兜底 |
| CVM 写操作 | 未执行 | 当前机器无 SSH 配置或私钥，未进行上传、迁移、构建或 PM2 重启 |
