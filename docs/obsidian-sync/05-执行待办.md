---
title: 执行待办
type: checklist
updated: 2026-08-26
tags:
  - next-steps
  - release
---

# 执行待办

## P0：让持久化认证可被线上证明

- [ ] 在已登录腾讯云 OrcaTerm 窗口完成一次子账号 MFA 验证后，再执行后端迁移、PM2 重启和真实账号烟测。

- [ ] 在 CVM 上传当前后端源码，生成 Prisma Client，执行 `prisma migrate deploy`。
- [ ] 发布 `dandan-frontend-dd-20260824-auth-persistence-final.zip` 内的静态文件，确认线上 `growth-school.html` 使用当前组件路径和认证客户端。
- [ ] PM2 加载最新环境变量并重启，确认 `/dd/health` 仍为 200。
- [ ] 用一次性测试目标完成验证码 -> 注册 -> `/api/users/me` -> refresh -> 旧 refresh 失效 -> logout 全链路。
- [ ] 浏览器关闭/重开或清空内存后验证 refresh cookie 自动恢复；不把账号密码写入任何 Web Storage。
- [ ] 分别用普通用户和管理员验证刷新后角色保留，并验证普通用户访问管理员能力仍被服务端拒绝。
- [ ] 找回密码完成后确认该用户全部旧会话失效。

- [x] 本地路由级验证：无联系方式时可用昵称 + 自设密码注册，并获得 refresh Cookie。
- [x] 本地验证管理员 refresh 后角色仍由服务端签发并保留。

> 本地已用 `auth-session-flow.test.ts` 覆盖同一链路；线上仍需 CVM 凭据和一次性测试目标验收。

## P0：让盲盒每个用户都能读写自己的数据

- [ ] 在线上确认 `buddy_*` 及 `buddy_feature_records` 迁移状态。
- [ ] 以两个测试用户验证偏好、推荐、消息、好友申请的用户隔离。
- [ ] 验证 `/api/buddy-box/features` 的写入、读取、`scope=public` 脱敏和幂等重复请求。
- [ ] 刷新主站和盲盒 iframe，确认同一 refresh cookie 下数据仍归属原用户。
- [ ] 保留所有 adapter 名称；接口错误必须回到 UI 错误态，不使用静默假成功掩盖 401/500。

- [x] 本地验证 33 个盲盒 adapter 均保留并通过统一用户归属接口。
- [x] 本地验证刷新盲盒页面时可恢复当前用户留言与玩法状态。

## P1：补齐高级玩法的真实业务语义

- [ ] 为试题盲盒建立题目、答案、审核和解锁状态的服务端状态机。
- [ ] 为抽盒、声望消耗、返还和奖励接入 `PointAccount`/`PointTransaction` 事务与幂等键。
- [ ] 为记忆盲盒到期解锁、反向盲盒认领、协作任务和安全冷却增加服务端权限和时间约束。
- [ ] 继续通过现有 `BuddyFeatureRecord` 兼容旧 adapter，但把关键结算从通用 JSON 记录升级为可审计领域操作。

## P1：生产短信与安全

- [ ] 配置腾讯云 SMS 生产 provider、审核签名和模板；敏感值只放 CVM secret 环境。
- [ ] 在受限测试号码验证发送记录、限流、失败提示和 provider 错误脱敏。
- [ ] 确认 `COOKIE_SECURE=true`、HttpOnly、SameSite、CORS origin；上游 `COOKIE_PATH=/api`，Nginx 使用 `proxy_cookie_path /api /dd/api`，不要改为只覆盖认证路径。

## P2：备份、监控与回滚

- [ ] 执行并验证 `server/deploy/backup-dandan-world.sh`。
- [ ] 在临时库恢复最新备份，记录恢复时间和校验结果。
- [ ] 配置健康检查、PM2、磁盘、数据库连接、验证码错误和 5xx 告警。
- [ ] 记录迁移 ID、部署包 SHA-256、回滚目录和演练结果，不记录密钥。

## 完成定义

## 授权发布阻塞

- [ ] 重新建立可见且可验证的 CVM shell 执行通道，确认 v5 后端和前端 zip 的远端 SHA-256。
- [ ] 在远端完成新后端目录的迁移、构建和 PM2 切换；保留旧目录。
- [ ] 备份并同步 `/var/www/dd` 后，用查询参数核对静态文件哈希。
- [ ] 仅在以上证据齐全后更新线上完成状态。

只有当 P0 的线上认证与盲盒隔离证据齐全、生产 Cookie 和迁移生效、退出/找回密码行为通过、备份可恢复时，才能把“上线可用”写入 [[00-项目总览]]；否则保持“代码完成，线上待验收”。

## 2026-08-25 状态更新

- [x] v5 前后端包远端 SHA-256 校验。
- [x] 新后端目录依赖、Prisma generate、迁移和构建。
- [x] 备份并同步 `/var/www/dd`，保留旧静态目录。
- [x] PM2 切换、保存和线上认证/盲盒 smoke。

历史上的“授权发布阻塞”已解除。后续仅剩 P1/P2 的 SMS 真机、备份恢复演练和高级玩法结算语义。

## 2026-08-25 联系方式可选版本

- [x] 后端目录 `/root/dandan-world-server-20260825-contact-optional` 已完成依赖安装、Prisma generate、迁移与构建。
- [x] PM2 已切换到该目录并保存；日志与本机健康检查已复核，不以 `online` 状态代替业务可用性。
- [x] 前端已同步至 `/var/www/dd`，并保留 `/var/www/dd.backup-contact-optional-20260825` 备份。
- [x] 生产认证 smoke：仅昵称加密码注册、获取当前用户、昵称加密码登录、注销和旧 access token 失效均已验证；临时测试数据已清理。
- [ ] 真实生产账号完成浏览器关闭后重开，验证 refresh Cookie 自动恢复。
- [ ] 使用两个生产测试用户继续验证盲盒私有数据隔离和 `scope=public` 脱敏。

## 2026-08-26 业务迭代

- [x] 本地完成任务、反馈、打听、通知 Prisma schema 与迁移草案。
- [x] 本地完成任务/反馈/打听/通知接口及后端敏感词双重校验。
- [x] 本地完成盲盒隐身推荐、好友状态机、30 分钟拒绝冷却、好友聊天权限与历史接口。
- [x] 本地完成响应式布局、访客首屏、任务/反馈 API 接入和 NEW 角标移除。
- [ ] CVM 备份后执行 `prisma migrate deploy`，单步读取输出并检查无 1045。
- [ ] 构建后重启 PM2，查看日志和 `/health`，再执行桌面/手机/平板线上 smoke。
- [ ] 用两个测试用户线上验证任务本人隔离、反馈本人历史、好友冷却和聊天权限。

## 2026-08-26 本地收口

- [x] 清理盲盒运行时代码中的本地 fallback adapter；缺少真实服务端插头时直接报错。
- [x] 清空盲盒收件箱和管理员反馈列表的静态测试初始数据，改为完全等待服务端返回；历史 demo 数据仍保留在数据库中。
- [x] 打听发布的 tags 与标题、正文一起执行后端敏感词双重校验。
- [x] 前端三个脚本语法检查通过；后端 `npm test` 为 12 个文件/47 个测试通过；`npm run build` 通过。
- [ ] 当前桌面浏览器自动化 wrapper 未返回 snapshot/screenshot 输出，不能据此宣称三端视觉验收完成。
- [ ] 线上仍按“备份 -> 迁移 -> 构建 -> PM2 日志 -> `/health` -> 三端 smoke”逐步执行。
- [x] 浏览器本地验收：主站访客首屏显示登录弹窗；盲盒直达页快照只含业务内容，窄视口截图未出现插件导航外壳。
- [x] 三张保留的开发测试发布卡补充统一 `DEMO` 标签，未删除原记录。
- [x] 个人蛋蛋币明细接入真实 point transaction 查询，服务端采纳回答结算与通知已接通。
- [x] 打听专属列表改用 `/api/inquiries/mine`，发布/采纳前端不再本地伪造余额变更。
- [x] 盲盒学校、省市区、管理员任务联系方式补充敏感词校验；接口失败改为用户可见错误提示。
- [ ] 为打听无人回答自动退回增加服务端定时任务与可审计退款流水；当前前端已停止本地伪造退回。
- [ ] CVM 备份、迁移、PM2 日志、`/health` 和三端线上 smoke 仍待执行。
- [x] 本地前后端交付包按最新源码重打包并完成 SHA-256 与禁入文件边界检查。

## 2026-08-26 任务结算补齐

- [x] 新增 `TaskClaim`、`Rating` 模型与 `202608260003_task_settlement` 迁移。
- [x] 认领、认领列表、确认配对、提交、验收结算、取消退款和评价接口完成幂等与权限校验。
- [x] 真实任务卡接入服务端认领/提交/取消/评价；余额以账户快照为准，开发 demo 卡不删除。
- [x] 管理员发布改为真实任务创建并审核，发布者认领列表改为 MySQL 读取。
- [x] 新增到期打听退款管理员入口，事务写退款流水和通知；待接入 CVM 受保护定时调用。
- [x] 本地验证 49 个测试、构建、Prisma validate、前端脚本语法均通过。
- [ ] CVM 先备份再执行新迁移；必须逐条读取输出，确认无 1045 后继续。
- [ ] PM2 重启后查看日志、调用 `/health`，再做桌面/手机/平板线上 smoke。

## 2026-08-26（今日盲盒行动匹配）

- [x] 推荐接口支持可选 `action` 查询参数。
- [x] `todayActions` 同值/包含关系命中时提升推荐排序。
- [x] 抽盒揭示后用选中行动刷新推荐列表。
- [x] 先红后绿补充后端和前端契约测试。
- [ ] 恢复 CVM 会话后执行生产备份、迁移状态核对、构建、PM2 日志、health 和三端 smoke。
- [x] 本地交付包重打包并完成文件数量、SHA-256、禁入文件边界检查。

## 2026-08-26（HTTP 轮询与 r7/r6 收口）

- [x] 消息抽屉 15 秒轮询与会话 5 秒轮询接入，关闭窗口清理定时器。
- [x] 本地 53 项测试、构建、Prisma schema、JS/内联脚本语法和差异检查通过。
- [x] 390x844、768x1024、1440x900 浏览器溢出检查通过，盲盒无独立外壳。
- [x] 重新生成前端 r7、后端 r6 包并核对 18/46 文件、SHA-256 和禁入文件。
- [ ] CVM 会话恢复后先备份，再逐条执行迁移、构建、PM2 日志、`/health` 和线上三端 smoke。

## 2026-08-26（最新收口）

- [x] 修复任务认领通知新增后测试环境未 mock 导致的 500；保留生产通知写入。
- [x] 重新运行 53 项后端测试、构建、Prisma generate/validate、前端语法、浏览器三端溢出检查和差异检查。
- [x] 重新生成并核对本轮前端/后端交付包（前端 r8 18 文件；后端 r7 47 文件；均无禁入文件）。
- [ ] CVM 恢复可验证会话后，按备份 -> 迁移 -> 构建 -> PM2 日志 -> `/health` -> 三端线上 smoke 执行。

## 2026-08-26 收口补丁

- [x] 修复主站首屏角色闪现，等待会话恢复后再显示 stage。
- [x] 管理员反馈回复补充前端敏感词前置校验；后端双重校验保持不变。
- [x] 开发蛋蛋币/经验历史记录保留并统一显示 `DEMO` 标签。
- [x] 推荐卡接入服务端好友关系状态；已是好友、待处理、拒绝冷却均显示明确状态。
- [x] 拒绝关系重新申请复用旧记录，禁止因方向变化重复写入关系记录。
- [x] 盲盒脚本改为有序 `defer`，可选宿主节点增加空保护。
- [x] 本地三端视觉/溢出验收：390x844、768x1024、1440x900。
- [x] 重新生成 r5 前端包和 r4 后端包，完成 SHA-256 与禁入文件检查。
- [ ] 生产仍按备份 -> 迁移 -> 构建 -> PM2 日志 -> `/health` -> 三端线上 smoke 执行；未完成前不标记上线。

## 2026-08-26（待生产发布）

- [x] 递归敏感词校验覆盖盲盒嵌套 JSON，注册/资料/反馈文本校验补齐。
- [x] 修复盲盒本地 API origin，移除高级玩法静态伪造结果。
- [x] 完成 55 项测试、构建、Prisma、前端脚本语法、差异和三视口结构验证。
- [ ] 恢复可验证 CVM 会话后，先创建并验证 MySQL 备份；数据库密码只从服务器环境文件读取，不记录到文档或终端输出。
- [ ] 执行 `prisma migrate deploy` 并读取完整输出；出现 MySQL 1045 时先核对完整 `DATABASE_URL` 的账号、密码、地址和数据库名。
- [ ] 构建并重启 PM2 后先查看日志，再请求 `/health`；不能仅根据 PM2 `online` 判定可用。
- [ ] 完成登录、任务、反馈、打听、好友、聊天和手机/平板/桌面的公网 smoke。

## 2026-08-26（交付包与生产发布）

- [x] 已生成前端 r10 和后端 r9 压缩包；已排除 `.env`、`.env.*`、`.cvm-app-password.local`、`node_modules` 与 `dist`。
- [x] 已核验包边界：前端 18 文件、后端 46 文件、禁入项 0；SHA-256 记录在验证文档。
- [ ] 在 CVM 先完成并核验 MySQL 备份；任何错误立即停止后续步骤。
- [ ] 注入完整 `DATABASE_URL` 后执行 `prisma migrate deploy`；如遇 MySQL 1045，先核对账号、密码、地址和数据库名，禁止反复重启 PM2。
- [ ] 构建、重启后先读取 PM2 日志，再请求 `/health`；仅 PM2 `online` 不可视为上线成功。
- [ ] 最后完成登录、任务、反馈、打听、好友和聊天的桌面/平板/手机公网 smoke。

## 2026-08-26（当前发布待办）

- [x] 已生成并核验前端 r11、后端 r10 交付包：18/46 文件、禁入项 0；包内不含 `.env`、密码文件、`node_modules` 或 `dist`。
- [ ] 在 CVM 验证数据库备份存在且可恢复后，读取当前迁移状态；本轮无新增迁移，不跳过状态核对。
- [ ] 执行构建和 PM2 reload 后先读取日志；出现 MySQL 1045 时先核对完整 `DATABASE_URL`，不反复启动进程。
- [ ] `/health` 成功后，进行登录、任务待审核、我的打听回复摘要、反馈、好友聊天的公网三端 smoke。

当前公网旧服务健康检查为 200，但版本特征证明 r11/r10 尚未上线。恢复 CVM shell 后，必须先验证远程目录与备份，再上传对应压缩包；禁止仅因为 `/health` 通过而跳过发布步骤。
