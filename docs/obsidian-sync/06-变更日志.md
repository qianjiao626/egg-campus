---
title: 变更日志
type: changelog
updated: 2026-08-26
tags:
  - changelog
---

# 变更日志

## 2026-08-24

- 补充认证路由级回归：无邮箱/手机号时可直接使用昵称和自设密码注册，仍由服务端签发 HttpOnly refresh Cookie；联系方式注册继续强制验证码。
- 盲盒高级玩法 adapter 全量接入 `buddy_feature_records` 用户归属持久化接口，保留原 adapter 名称和会话刷新行为。
- 增加 `GET /api/buddy-box/features` 用户记录列表接口；盲盒刷新时恢复当前用户留言和已完成玩法状态，不再只依赖当前页面内存。

- 盲盒模式最终收口为纯内容组件：直接访问组件地址只输出盲盒业务内容，不再提供插件独立完整页面模式。
- 主站盲盒入口改用相对组件路径，避免本地预览与 `/dd` 子路径部署之间的路径差异；主站容器负责滚动。
- 本地最终验证：9 个测试文件、34 个测试通过，TypeScript 构建通过，盲盒脚本语法检查通过。
- 本轮只生成部署包，未宣称已完成 CVM 线上迁移或重启。
- 增加 `server/tests/auth-session-flow.test.ts`，覆盖注册事务、refresh Cookie 轮换、`/users/me`、logout 和旧会话失效；当前本地总计 10 个测试文件、35 个测试通过。
- 修复 Cookie Path：生产公开路径必须覆盖 `/dd/api`，否则盲盒 `/dd/api/buddy-box/*` 无法收到登录 Cookie；Nginx 已改为 `proxy_cookie_path /api /dd/api`。

- 盲盒插件完成纯内容化：删除插件顶部、侧边栏、页脚和独立标题；主站侧边栏直接在右侧容器加载 `/dd/blind-box/`。
- 盲盒保留 MBTI、资料、兴趣、今日想法、抽盒、推荐、消息、好友申请和玩法中心；主站容器负责滚动。
- 盲盒组件 CSS 清理历史外壳选择器，并取消玩法弹窗的独立滚动；生成 `output/releases/dandan-frontend-dd-20260824-content-component-final.zip`。
- 收紧 refresh Cookie 轮换测试，并生成待 MFA 后发布的精简后端包 `output/releases/dandan-server-deploy-20260824-mfa-ready-v2.zip`；包内不含 `.env`、`node_modules`、`dist` 或本地凭据文件。
- 认证代码已具备注册、登录、验证码、刷新、退出、找回密码和 `GET /api/users/me`。
- 会话使用 HttpOnly refresh cookie，access token 只在内存中；不持久化密码、refresh token 或验证码。
- 盲盒基础数据已接入 `buddy_preferences`、`buddy_boxes`、`buddy_messages`、`buddy_friend_requests`。
- 新增 `BuddyFeatureRecord` 迁移和 `/api/buddy-box/features` 通用读写路由，前端高级玩法 adapter 已接入该接口。
- 本地验证：后端 8 个测试文件、30 个测试通过；TypeScript 构建通过；盲盒两个脚本语法检查通过。
- 仍待完成：最新后端在线迁移/重启后的完整认证 smoke、浏览器跨刷新验证、真实腾讯云 SMS、备份恢复演练和高级玩法结算语义。

## 记录规则

后续每次部署、schema 迁移、认证策略或盲盒接口变更，追加日期条目，并链接到 [[04-验证记录]] 和 [[05-执行待办]]。不要在日志中粘贴环境变量值、密码、token、验证码或短信密钥。

## 2026-08-25（v5 线上发布完成）

- MFA 完成后继续传输前端 v5，远端 SHA-256 与本地一致；后端包此前已核对一致。
- 新后端隔离目录完成依赖安装、Prisma generate、Prisma migrate deploy 和构建；数据库 schema up to date。
- 前端同步前保留 `/var/www/dd.backup-auth-persistence-v5`；未修改根站“航线”。
- PM2 `dandan-world` 切换到新目录并保存进程列表。
- 生产 smoke 通过：注册、refresh、`/users/me`、盲盒 features、退出；公开页面和匿名接口状态符合预期。

## 2026-08-25（工作流约定）

- 新增全局执行要求：每个项目任务开始前，先在 GitHub 检索与任务相关的 skills，评估后仅安装确有价值且来源可信的 skill。
- 每个任务完成后必须更新本目录的 Obsidian 文档，记录变更、验证结果、遗留风险和可复用经验。
- skill 安装遵循 Codex `skill-installer` 流程；不下载与当前任务无关的工具，不记录密码、token、验证码或云密钥。
- 本次按新流程检索 GitHub curated skills；现有 `playwright`、`playwright-interactive`、`screenshot`、`cloudflare-deploy` 等已安装能力覆盖当前项目，未新增无关 skill。

## 2026-08-25（GitHub 协作仓库准备）

- 项目根目录初始化 Git，默认分支设为 `main`。
- 新增根级 `.gitignore`，排除 `node_modules`、构建产物、`.env`、本地 CVM 密码文件、部署 staging、压缩包和截图；源码、迁移、测试与 Obsidian 文档已完成首轮暂存。
- 已扫描暂存内容，未发现真实云密钥、私钥或生产连接凭据；`.env.example` 和测试中的值均为占位符/测试值。
- GitHub 浏览器当前未登录，本机也无 GitHub CLI 凭据；待账号登录和 `egg-campus` 仓库地址确认后再提交并推送。

## 2026-08-25（GitHub 首次推送完成）

- 已确认仓库为公开仓库 `qianjiao626/egg-campus`，默认分支 `main`。
- 保留远程初始提交并合并本地源码；首个源码提交为 `4e8a9da`，合并提交为 `227ed2d`。
- 已推送完整协作源码：主站/盲盒前端、Fastify 后端、Prisma schema 与迁移、测试、部署脚本和 Obsidian 文档。
- 新增根目录 `README.md`，说明目录、启动方式、部署边界和协作约定；远程 `main` 当前提交为 `f597a49`，本地与远程一致。
- 推送过程中发现本机 Git Credential Manager 曾缓存账号不匹配；通过官方浏览器设备授权重新绑定后推送成功，未读取或记录任何凭据。
- 经验：首次协作仓库必须先核对 GitHub 账号/仓库 owner，再检查 `.gitignore` 和 staged 文件；远程已有初始化提交时，使用保留远程历史的合并提交，不覆盖远程内容。

## 2026-08-25（后端源码包）

- 生成可交付后端源码包：`output/releases/dandan-server-source-20260825.zip`。
- 包含后端源码、Prisma schema 与全部迁移、测试、部署脚本、运行文档、`package.json` 和锁文件，共 39 个文件。
- 明确排除 `node_modules`、`dist`、`.env`、`.cvm-app-password.local` 和日志；压缩包清单复核无禁入项。
- 交付包 SHA-256：`958F9860569AB2C45A12498EC8B0A51E091E8385CFEDC0B517B7571CF9053990`。
- 新经验：源码交付包应与 Git 协作仓库分离，生成包放在 `output/releases`，仓库只保留可复现源码和锁文件，避免把本机构建结果或凭据带入协作历史。

## 2026-08-25（WorkBuddy 交接协议）

- 新增 [[08-WorkBuddy实时协作协议]]，明确 GitHub PR、Obsidian 任务收件箱和用户消息三种交接方式。
- 约定 GitHub 是代码事实源，Obsidian 是任务/验证/经验事实源，用户消息负责授权和冲突裁决。
- 新增结构化任务模板：WorkBuddy 使用 `TASK-xxx`、`READY_FOR_CODEX` 状态和明确验收标准，用户将任务编号交给 Codex 后再执行。
- 经验：共享文件不等于后台实时通信；必须通过分支、commit、PR 或任务文件留下可读取证据，才能避免口头交接丢失上下文。

## 2026-08-25（TRAEWORK 精简提示词）

- 新增 [[10-TRAEWORK精简提示词]]，压缩为仓库、边界、执行、验证和交接五部分，适合直接粘贴到 TRAEWORK。

## 2026-08-25（注册联系方式可选）

- 注册调整为昵称 + 密码即可完成；邮箱和手机号继续支持登录，但均为可选联系方式。
- 注册验证码改为可选：填写有效验证码时验证并标记联系方式；不填写时保存联系方式但保持未验证状态。
- 保留验证码发送/校验接口和找回密码验证码流程，不改动数据库字段或迁移。
- 前端注册提示同步更新，验证码仅在用户填写验证码时触发校验。
- 本地验证：10 个测试文件、43 个测试通过；后端构建、前端 `api-client.js` 语法检查和工作区 diff 检查通过。

## 2026-08-25（联系方式可选版本线上发布）

- 后端已发布至隔离目录 `/root/dandan-world-server-20260825-contact-optional`；依赖安装、Prisma Client 生成、4 个迁移核验和构建完成，数据库无待执行迁移。
- PM2 `dandan-world` 已切换并 `pm2 save`；日志无 MySQL 1045、未处理异常或启动失败，本机与公网健康检查均为 200。
- 前端已同步到 `/var/www/dd`，发布前备份为 `/var/www/dd.backup-contact-optional-20260825`；未修改根站“航线”及其 Supabase 配置。
- 发布包 SHA-256：后端 `28B22AF4993D264795DC906529F0FBF6FC85B033695517CEC7CF8B0E49D97395`，前端 `89884703C9951FC770F1376B51E7CD0549681D9295BC309AEF0567F51E320E`。
- 生产认证 smoke 通过：仅昵称与密码注册、`/api/users/me`、昵称密码登录、注销和旧 access token 失效；临时测试用户及其 2 条会话、用户统计、积分账户和积分流水均已按外键依赖清理。
- 数据库备份 `/root/backups/dandan_world/dandan_world-20260825103836.sql.gz` 已完成可读性验证；不在文档中记录数据库连接字符串、密码、令牌或验证码。
- 经验：PM2 显示 `online` 后仍必须先查看日志和真实 `/health`；清理测试用户前先处理外键依赖，不能通过关闭外键约束绕过数据完整性。

## 2026-08-26（业务迭代本地完成）

- 新增业务迁移与 Prisma 模型：任务状态 `pending_review/approved/completed/needs_revision`、反馈、打听回复、通知。
- 新增后端接口并应用统一敏感词校验，普通用户反馈历史按当前用户隔离，管理员操作服务端鉴权。
- 盲盒推荐现在过滤隐身偏好并返回匹配信息；好友申请不再 upsert 覆盖旧拒绝，加入好友已存在/待处理/拒绝冷却状态；非好友禁止聊天。
- 新用户账户与首条流水改为 `+100`，备注为“登录获赠蛋蛋币 +100”，不清理既有 demo 明细。
- 前端主站补充移动/平板布局、访客登录首屏、真实任务/反馈 API 插头，盲盒敏感词提示不暴露具体词语并移除 `NEW` 角标。
- 验证：47 个后端测试通过，TypeScript 构建和三个前端脚本语法检查通过。
- 风险：生产迁移和 PM2 发布尚未执行，必须在 CVM 备份、逐步迁移、查看日志后再上线。
- 前端任务复核：移除无任务 ID 的静态发布卡片，按 `userId` 隔离并重绘本人任务；审核通过/退回改走 `/api/tasks/:id/review`，退回状态显示为“待修改”。

## 2026-08-26（本地收口与验证）

- 删除盲盒 `app.js` 中注释包裹的 fallback adapter，保留真实 `buddyBoxApi`/`buddyBoxDataAdapter` 接口插头。
- 盲盒收件箱、反馈管理列表不再预置本地测试对象，页面启动时只使用 MySQL API 返回；数据库中的历史 demo 流水不受影响。
- 打听创建接口现在对 `tags` 同步执行敏感词校验。
- 验证证据：三个前端脚本 `node --check` 通过；后端 `npm test` 12/12 文件、47/47 测试通过；`npm run build` 退出码 0。
- 浏览器 wrapper 在本桌面会话未返回 snapshot/screenshot，视觉验收保持未完成状态。
- 随后使用 `agent-browser` 连接现有 Chrome CDP 完成验证：本地主站访客登录弹窗可见；`/blind-box/` 标题为“盲盒交友内容组件”，快照不含插件顶部、侧栏和页脚；窄视口截图已保存到 `output/playwright/blind-box-content-current.png`。
- 保留的三张开发测试发布卡统一显示 `DEMO` 标签；前端交付包已用最新源码重打包。
- 当前交付包：`dandan-frontend-dd-20260826-business-iteration.zip` SHA-256 `62DE29A124A453EFF9F82AD0F1E51F04047D10C21B34E894A2636D76D5731F42`；`dandan-server-deploy-20260826-business-iteration.zip` SHA-256 `6F0294C5BD333DC71066E60DE32CEBB3C0DE14E19A0A9B4ACAAF75717B5130C1`。

## 2026-08-26（真实流水与打听结算收口）

- `api-client.js` 新增 `pointTransactions()`，个人明细在登录后优先从 `/api/users/me/point-transactions` 读取 MySQL 流水；保留 demo 数据仅作为未登录/接口失败时的展示兜底，不生成新假流水。
- 打听“我的打听”改用 `/api/inquiries/mine` 做用户边界校验；新增 `POST /api/inquiries/:id/adopt/:replyId`，服务端事务校验发布者、回答归属、重复采纳，并向回答者发放蛋蛋币和通知。
- 打听发布不再在前端直接扣蛋蛋币；余额以服务端 point account 快照为准。自动退回不再由前端伪造余额或悬赏状态，后续由服务端定时任务接管。
- 盲盒偏好保存增加学校、省市区文本过滤；清理“模拟/预览”文案，接口同步失败改为节流错误提示，不再静默显示假成功。
- 管理员发布任务和认领联系方式补齐前端敏感词校验，仍不显示具体命中词。
- 复用经验：涉及余额的动作必须“服务端事务 -> 账户快照 -> 流水明细”闭环；所有失败路径必须有明确错误态，不能以 preview 语义掩盖接口断连。
- 最新交付包：`output/releases/dandan-frontend-dd-20260826-business-iteration-r2.zip` SHA-256 `A5F2A7082844ED9FA520442FF4EFE57D5D9B9EFE6BA7E43856F1EB32B9BC31AC`；`output/releases/dandan-server-deploy-20260826-business-iteration-r2.zip` SHA-256 `87D6E4EDC6A7428648BA5A1AACB179489D30FB1994F4C4BF264DC5E402D58C47`。

## 2026-08-26（任务结算与评价）

- 新增 `TaskClaim`、`Rating` 模型和 `202608260003_task_settlement` 迁移，保留原任务 UI 和 demo 卡片。
- 任务接口新增认领、认领列表、确认配对、提交、验收结算、取消退款、评价；复合唯一索引和服务端状态机防重复写入。
- 前端真实任务操作改走 API 并以服务端账户快照刷新蛋蛋币；只有无任务 ID 的开发卡保留本地演示逻辑。
- 管理员发布改为真实创建并审核任务；发布者确认配对时认领者列表从 MySQL 加载，确认后服务端写入 assigned 状态和通知。
- 新增受保护的 `POST /api/admin/inquiries/refund-expired`，用于定时处理无人回答到期打听的退款、状态和通知。
- 验证结果：49 个测试通过，TypeScript 构建、Prisma validate、3 段主站内联脚本和 API/盲盒脚本语法均通过。
- 线上待办：备份 -> `prisma migrate deploy` -> 构建 -> PM2 日志 -> `/health` -> 桌面/手机/平板 smoke；未执行前不写线上完成。

- 最终交付包：前端 `output/releases/dandan-frontend-dd-20260826-business-iteration-r4.zip`（18 个文件，SHA-256 `16C5F92C24388BF6A077A08493DCD97A67918FFE8879334A1E5F84650BBAE784`）；后端 `output/releases/dandan-server-deploy-20260826-business-iteration-r3.zip`（45 个文件，SHA-256 `B70DD8EFF8805DFA989FEAFA287B9DFFF4F096892F4239913A6B0416F32C4D5D`）。两包均排除 `.env`、本地密码、`node_modules` 和 `dist`。

## 2026-08-26（收口补丁 r5/r4）

- 主站首屏改为会话恢复后显示，消除静态管理员页面闪现。
- 管理员反馈回复增加前端敏感词拦截；提示仍不显示具体词语，服务端过滤器保持第二层防线。
- 个人中心静态开发蛋蛋币/经验记录增加 `DEMO` 标记，记录本身不删除；真实 MySQL 流水保持原样。
- 盲盒推荐接口批量返回好友关系状态，前端按状态显示“已是好友”“已有待处理请求”“对方已拒绝”；接受好友前聊天按钮禁用。
- 好友申请在历史反向拒绝关系过期后复用原记录，避免创建新的反向重复关系。
- 盲盒数据/API/业务脚本按顺序 `defer`，并为缺少消息铃铛或消息计数的宿主节点提供空保护；不影响完整主站宿主。
- 本地三端验收无横向溢出：390x844、768x1024、1440x900；截图写入 `output/playwright/`。
- 新包：前端 `output/releases/dandan-frontend-dd-20260826-business-iteration-r5.zip`，SHA-256 `914E08085616E8E603810A394A82776DC064D142E2CA61A74DA1D1832F7EC8F9`；后端 `output/releases/dandan-server-deploy-20260826-business-iteration-r4.zip`，SHA-256 `48DD1E9B8E5E40F8D47B93B0CD6C65985E750B15B750554A45C2668F6112006D`。
- 经验：静态资源加载时序要按内容组件的最小 DOM 契约处理，不能假设所有宿主都提供完整导航节点；生产发布仍必须按备份、迁移、构建、日志、健康检查顺序推进。

## 2026-08-26（今日盲盒行动匹配）

- `server/src/app.ts` 的 `/api/buddy-box/recommendations` 新增可选 `action` 参数，按 `todayActions` 同值或互相包含关系加权排序。
- `backend-handoff-package/blind-box/buddy-box-api.js` 支持 URL 编码行动参数；`app.js` 在盲盒揭示后重新同步推荐。
- 保留原有 MBTI、兴趣、隐身过滤、好友关系状态、聊天权限和数据库字段，不新增迁移。
- 经验：盲盒抽取结果要进入后续推荐请求，否则抽取行为与匹配体验断开；可选参数可保持旧页面首次加载兼容。
- 重新生成前端 r6、后端 r5 交付包；保持不包含环境文件、本地密码、依赖目录和构建产物。

## 2026-08-26（HTTP 轮询）

- `backend-handoff-package/blind-box/app.js` 新增收件箱 15 秒轮询和会话 5 秒轮询，生命周期关闭时清理计时器。
- 好友申请接受后立即重新同步推荐关系状态，减少“已接受但卡片仍可申请”的短暂不一致。
- 本地完整验证达到 53 个测试通过；交付包更新为前端 r7、后端 r6，均通过边界检查。

## 2026-08-26（任务认领通知测试兼容修复）

- 任务认领接口新增通知写入后，契约测试首个认领请求因未 mock `prisma.notification.create` 返回 500；补齐隔离测试 mock，未改变生产路由和通知逻辑。
- 最新验证：后端 12 文件/53 测试通过，TypeScript 构建、Prisma generate/validate、三个前端脚本和主站内联脚本语法通过；盲盒纯内容页在手机、平板、桌面视口无横向溢出。
- 生产仍保持待执行，必须取得可验证 CVM 会话后按备份、迁移、构建、PM2 日志、健康检查和线上 smoke 顺序发布。
- 经验：新增副作用调用时，契约测试必须显式 mock 对应持久化边界，避免测试越过隔离层连接真实数据库。
- 本轮交付包已重新生成：前端 r8 SHA-256 `FEF8F2666465476D16C0A3117A909BA0F607FF0FDDAD75FC1D696E40E9E2F`；后端 r7 SHA-256 `7FDB9C4CFD179AE59A0E5082CFD1D8D5835C10B2F9222FC572C7A6D1F5AEA637`。

## 2026-08-26（敏感词递归校验与盲盒开发适配）

- 新增 `server/src/content-filter.ts` 的递归 JSON 检查能力，并用于 `POST /api/buddy-box/features`；嵌套字符串不再能够绕过服务端敏感词拦截。
- 注册、用户资料更新、反馈提交的全部可写文本字段补齐统一敏感词校验；用户提示始终不显示具体词语。
- `blind-box/buddy-box-api.js` 在本地开发自动请求 `http://127.0.0.1:3310`，生产保持 `/dd`，允许部署方通过 `window.DANDAN_API_ORIGIN` 覆盖。
- `blind-box/app.js` 删除高级玩法无服务端记录时的静态结果伪造；兴趣雷达缺少统计时显示真实空状态。
- 新增内容过滤和高级玩法契约测试，本地总计 55 项测试通过；生产部署未执行。
- 重新生成前端 r9 与后端 r8 交付包：前端 18 文件、后端 46 文件，SHA-256 与禁入文件边界已复核；生产仍未发布。

## 2026-08-26（盲盒真实结果与好友并发保护）

- `server/src/app.ts`：高级玩法结果不再构造固定破冰话题、校园活动或安全状态；无服务端记录时返回空数据或 pending 状态。
- `server/src/app.ts`：好友申请在事务内锁定双方用户行，并保留 `FRIEND_EXISTS`、`REQUEST_PENDING`、`REJECT_COOLDOWN` 和唯一约束的处理，防止并发双向申请写出重复关系。
- `blind-box/buddy-box-api.js`：雷达无数据时显示“暂无服务端统计”。
- `blind-box/app.js`：AI 破冰无数据时显示“暂无服务端破冰话题”。
- 本地复验通过；生产未执行，不变更航线项目或 Supabase。
- 交付包更新为前端 r10（18 文件，SHA-256 `D33240081899937EC2544A746408990CE743B2C8FD60E7CF7F0B1CD6E55EA584`）与后端 r9（46 文件，SHA-256 `19A5BFA7F9E6BDF2504A23DE22F8CC468AC229FA041AE23E8E4C9464FA57EABF`）；均排除环境文件、密码文件、依赖目录和构建产物。

## 2026-08-26（我的打听回复汇总与发布表单修正）

- `server/src/app.ts`：`GET /api/inquiries/mine` 在保持原字段的前提下增加 `replyCount` 和 `recentReplies`，只读取当前用户帖子的其他用户回复。
- `growth-school.html`：我的打听面板使用服务端汇总字段展示最新回复摘要；不再依赖全站列表筛选来充当消息汇总。
- `growth-school.html`：普通用户发布任务移除标题、描述的开发示例文本兜底，空字段不会写入 MySQL。
- `server/tests/business-contract.test.ts`、`server/tests/frontend-auth-contract.test.ts`：新增回归契约，当前全量测试为 58 项通过。
- 交付包更新为前端 r11（SHA-256 `FD51BB38499DF6531E93F9A46FA92AD9F80CBF1376E564B3165E4A5DA4700968`）和后端 r10（SHA-256 `61916188C3E1FA9CD844F9F5A5D3AC6B54AA9099488F62F333404B6B284639AD`）。

## 2026-08-26（协作仓库推送与发布阻塞）

- 本地提交 `c523b08` 已推送至 GitHub 仓库 `qianjiao626/egg-campus` 的 `main` 分支，工作区保持干净。
- 已通过腾讯云控制台进入 OrcaTerm；当前 CVM 会话被 MFA 动态码验证拦截，生产发布尚未执行。
- 待用户在腾讯云身份验证弹窗完成当前 MFA 后，再按备份、上传、迁移、构建、PM2 日志、健康检查和公网 smoke 顺序逐步执行。
- 经验：源码推送成功不等于生产上线；必须以远程版本特征、PM2 日志、`/health` 和关键流程 smoke 共同验收。

## 2026-08-26（完整敏感词词库与匹配资料收口）

- 后端内容过滤器改为加载随源码发布的完整中文词库，并以 Trie 扫描处理全角、大小写及常见标点/空格拆分；拦截提示始终不泄露命中词。
- 为避免公开词表中的通用短词误伤正常校园内容，保留既有高风险短词并过滤已验证的业务通用词；注册、任务、打听、反馈、盲盒消息和嵌套玩法 JSON 仍统一走服务端最终校验。
- 前端新增同词表预检脚本，主站与盲盒组件在写入请求前复用，后端仍保留不可绕过的二次校验。
- 盲盒推荐 API 返回用户已保存的个人简介，推荐卡和资料面板优先展示该真实资料；好友与聊天的既有 MySQL 读写逻辑不变。
- 验证：后端全量 `64/64` 测试、TypeScript 构建、Prisma schema 校验、前端三个脚本语法检查均通过。腾讯云 OrcaTerm 仍等待 MFA，生产未发布。

## 2026-08-26（r12/r11 交付与协作状态）

- 发布包已更新为 `output/releases/dandan-frontend-dd-20260826-business-iteration-r12.zip` 和 `output/releases/dandan-server-deploy-20260826-business-iteration-r11.zip`，分别含 19 和 47 个文件；归档内容已检查，不包含环境变量、密码文件、依赖目录或构建产物。
- SHA-256：前端 `D95A1F9CD9FEC1DD61501EFF681EEBDBE17DA1E0C70DB292D09F906873A7C26D`；后端 `986FA8A36F7463952BE4F2BF6647F8D153315517A1D0AD243DCD823525C90DB9`。
- 本地 Git 提交 `6d1619f` 已创建；推送时因工作机无法连接 `github.com:443` 失败，未将其误记为已同步。
- OrcaTerm 仍停在腾讯云 MFA 身份验证窗口，未执行任何生产备份、迁移、上传、PM2 重启或数据库写操作。

## 2026-08-26（TDD 收口与响应式修复）

- DEMO 蛋蛋币流水抽为 `DEMO_POINT_LOGS`，服务端真实流水加载后与 DEMO 合并展示；DEMO 记录不写回 MySQL、不被清空，并继续显示 `DEMO` 标签。
- 打听评论服务端校验 `parentId` 必须属于同一打听且父记录必须是回答；跨打听评论直接返回 400，避免污染回复关系。
- 打听回复通知补齐回复者昵称、打听标题、回复 ID 和打听 ID；前端保留通知 payload/refId，通知可直接打开对应打听。移除回答/采纳时错误的本地假通知，服务器通知作为唯一真实来源。
- 打听墙对用户可控标题、正文、昵称、回答和评论统一 HTML/内联参数转义；不改变敏感词过滤和数据库接口。
- 小屏布局补齐任务双栏、广场卡片、任务条、固定右栏、我的任务 Tab、个人角色卡和弹窗宽度的响应式覆盖，避免手机/平板横向溢出。
- 验证：`npm test` 12 个文件、67 项通过；`npm run build` 通过；主站内联脚本语法通过；`git diff --check` 通过。生产仍等待腾讯云 MFA，未执行远端写操作。
- 发布包已重新生成并复核：前端 `output/releases/dandan-frontend-dd-20260826-business-iteration-r13.zip`（19 文件，SHA-256 `BF926AA8BB893C0045169E0A8546FC85CD7C5FC2F0B56EF94E535BA3EA7CF309`）；后端 `output/releases/dandan-server-deploy-20260826-business-iteration-r12.zip`（47 文件，SHA-256 `4D91F1A797BA9E7E8C5CF59B2C113E619E911DC5A7B5BCBEC70DE8D9867BCB51`）。两包均不含 `.env`、密码文件、`node_modules` 或 `dist`；前端敏感词过滤脚本已确认入包。

## 2026-08-26（好友状态、我的打听与前端双重校验收口）

- 盲盒 iframe 前端直接调用完整 `DandanSensitiveFilter`；注册昵称、学校、专业、城市、年级在请求前补齐同一套文字预检，服务端二次校验保持不变，提示不显示具体词语。
- 盲盒收件箱对昵称和留言做 HTML 转义；好友申请新增拒绝按钮并复用真实 `/reject` 接口，移除本地伪造好友/聊天消息；空推荐会清除旧卡片。
- 盲盒学校资料改为写入当前用户资料接口；地区数据通过 `regionReady` 完成初始化后再恢复，避免异步选项覆盖已保存选择。
- 推荐关系同状态时按 `updatedAt` 选择最新记录，避免旧拒绝记录绕过 30 分钟冷却；聊天历史改为读取最近 100 条后恢复正序。
- 学生侧边栏新增独立“我的打听”页面，复用 `/api/inquiries/mine` 的当前用户过滤和回复摘要；八卦页原折叠入口保留兼容。三张开发演示任务卡标记 `data-demo="true"`，同步真实任务时保留并继续显示 `DEMO`。
- 新增前端契约覆盖敏感词入口、好友拒绝、我的打听路由、通知跳转和 DEMO 保留边界。
- 验证：`npm test` 12 个文件、72 项通过；`npm run build` 通过；主站和盲盒脚本 `vm.Script` 语法通过；`git diff --check` 通过；盲盒 390x844 Chromium smoke 正常渲染。主站静态 smoke 因本机未启动 `3310` 后端停在会话恢复前，不替代线上健康检查。
- 已生成可部署包：前端 `output/releases/dandan-frontend-dd-20260826-business-iteration-r14.zip`（19 文件，SHA-256 `44039FB98F17D4F712B123C0E28AD1D98C5A5612A33DF20B7CDCBA0D0F82FA09`）；后端 `output/releases/dandan-server-deploy-20260826-business-iteration-r13.zip`（47 文件，SHA-256 `0581C20E8F416F84CC74F609EB70E44EDCD68B245123C1F0DD2D2DA006BDBDED`）。两包均排除环境文件、密码文件、`node_modules` 和 `dist`，盲盒目录层级已复核。
- 生产仍未发布：CVM MFA 会话恢复后必须按备份、迁移、构建、PM2 日志、`/health` 和三端公网 smoke 顺序执行。

## 2026-08-26（最终交付与生产门禁收口）

- 通知中心邀请技能标签、内联事件参数和时间字段统一做 JS/HTML 双层转义；补充盲盒内容契约断言，避免用户可控通知内容注入。
- `server/cvm-deploy.sh` 增加生产前置门禁：拒绝尾部反斜杠、缺少密码/主机/端口/库名的 `DATABASE_URL`、公网监听 HOST、localhost CORS 和 Mock 验证码 provider。
- `server/cvm-auth-smoke.sh` 改用 URL 解析并解码数据库密码，支持 URL 编码的特殊字符，仍强制校验 `app_user@127.0.0.1:3306/dandan_world`。
- 最终本地验证：`npm test` 12 个测试文件、72 项通过；`npm run build` 通过；三个前端脚本、主站 2 段内联脚本通过语法校验；`git diff --check` 通过。
- Chromium 三视口截图已复核：`output/playwright/blind-box-mobile-final.png`（390x844）、`blind-box-tablet-final.png`（768x1024）、`blind-box-desktop-final.png`（1440x900）；直达盲盒页无顶部导航、侧栏、页脚和 `New` 角标，无横向溢出。
- 新交付包：前端 `output/releases/dandan-frontend-dd-20260826-business-iteration-r15.zip`（19 文件，SHA-256 `8A46347AEA8BEE056F99B9AD2E86B05A6168721E7E3B50BDDDA9ABBDD1B4D089`）；后端 `output/releases/dandan-server-deploy-20260826-business-iteration-r14.zip`（47 文件，SHA-256 `EBAA0C87122B56C9E6D741DCD0E4E911B7CA8CE01682DD775897F2431DB2D519`）。两包均排除 `.env`、密码文件、`node_modules` 和 `dist`。
- 生产状态仍为待发布：腾讯云 CVM MFA 会话未恢复，未执行备份、迁移、上传、PM2 重启或公网 smoke；“航线”与 Supabase 未读取或修改。
- 本轮最终提交 `aed62ef` 已成功推送到 GitHub `qianjiao626/egg-campus` 的 `main` 分支，供 WorkBuddy/多人协作继续使用。

## 2026-08-26（注册、任务频控、真实初始化与排行榜修复）

- 注册表单移除手机号和注册验证码字段；邮箱仍为可选联系方式，后端保留历史手机号字段与手机号登录兼容。
- `UserStats` 新增经验值、每日发布日期和每日发布次数；`Task` 新增固定发布经验奖励。服务端事务锁定统计行，每日最多发布 10 次，奖励按第 1 至第 10 次 `10..1` 递减，第 11 次返回 `TASK_DAILY_LIMIT_REACHED`。
- 管理员审核只在任务状态首次转为 `approved` 时发放固定经验，重复审核不会重复加经验；前端真实任务不再自行增加经验或蛋蛋币，DEMO 任务保留但不写入真实账户。
- 新注册用户的认领任务列表只接受后端真实记录，历史静态 DEMO 卡片不会注入真实用户；空状态明确显示没有已认领任务。
- 新增 `/api/users/leaderboard`，仅聚合 `active` 的真实学生；普通分类按真实经验/属性/蛋蛋币排序，八卦榜按真实回答点赞与采纳聚合。前端不再以静态虚假用户数组作为排行榜数据源。
- 新增迁移 `server/prisma/migrations/202608260005_task_publish_limits` 与规则单元测试；本地验证 `npm test` 12 个文件/78 项通过，`npm run build`、Prisma generate 和前端脚本语法检查通过。
- 经验：限频必须与计数更新放在同一事务并锁定统计行；前端 DEMO 数据可以保留用于开发展示，但不得参与真实账户结算、排行榜或新用户初始化。
- 生产状态：本轮迁移、备份、PM2 重启和公网 smoke 尚未执行，等待可验证 CVM MFA 会话后按门禁顺序发布。
- 本轮交付包：`output/releases/dandan-frontend-dd-20260826-task-bugfix-r1.zip`（SHA-256 `FF8597D0B4AC0B0C28405332AC326821F01C1C3B8FCA5DD7AF463CAAB458BCBC`）；`output/releases/dandan-server-deploy-20260826-task-bugfix-r1.zip`（SHA-256 `EE65F055ED684AA856D82812864F69B94256EEB254AF40EBA9055236478CB2AF`）。后端包检查确认不含环境文件、本地密码、`node_modules` 或 `dist`。

## 2026-08-26（关机恢复与技能调用修复）

- 关机后首次故障根因：本地执行宿主短暂未恢复、agent-browser daemon 状态文件过期，以及 Windows Chrome 冷启动缺少 `--no-sandbox`。
- 已确认 `C:\Users\梁惠\.codex\.sandbox-bin\codex-code-mode-host.exe` 恢复存在；`agent-browser doctor --fix` 已重新生成加密状态密钥并清理过期 daemon 状态。
- 已修复 `C:\Users\梁惠\.agent-browser\config.json`：`args` 使用字符串 `--no-sandbox`，`idleTimeout` 设为 `0`，避免更新或关机后再次因默认启动参数失败。
- 已清理空的 `animation-systems.partial-install` 中断目录；技能发现不再看到未完成的顶层目录。
- 新增 `docs/skills-healthcheck.ps1`，检查两个技能根目录、skill frontmatter、官方插件 manifest、执行宿主和 agent-browser 配置。
- 验收：Codex skill 74 份、Agents skill 57 份、官方插件 5 个；健康检查输出 `SKILLS_HEALTH_OK`；agent-browser 冷启动打开 `about:blank`、`https://example.com` 并完成快照/截图，截图 `output/skills-cold-start.png`。
- 经验：关机恢复后先修复本地执行层和浏览器启动层，再恢复云端操作；工具链健康不等于业务上线，生产仍需完成构建、PM2 日志、health 和公网 smoke。

## 2026-08-26（工具链重试复核）

- [x] 再次执行 `docs/skills-healthcheck.ps1`，两个技能根目录、frontmatter、官方插件 manifest、执行宿主和 agent-browser 配置均通过，输出 `SKILLS_HEALTH_OK`。
- [x] 再次确认技能数量为 Codex 74 份、Agents 57 份，未发现 `.partial-install` 遗留目录。
- [x] 再次调用 `agent-browser skills get core` 并完成 `connect/open/get title/get url/snapshot/screenshot` 烟测；Windows Chrome 使用 `--no-sandbox` 冷启动配置正常。
- [x] `git diff --check` 通过；临时核心指南输出已清理。
- [ ] 腾讯云浏览器会话当前需要用户重新完成登录和 MFA；该认证状态不属于本地 skills 修复范围，完成认证后才能继续 CVM 发布。

## 2026-08-26（取消手机号登录与短信配置）

- 后端登录查询移除手机号条件，仅支持邮箱或昵称；手机号、`verifiedPhoneAt` 和数据库历史字段保留，不做破坏性删列。
- 验证码通道收紧为邮箱；注册手机号不再触发验证码或写入手机号验证时间，找回密码仅支持邮箱验证码。
- 移除腾讯云 SMS provider 和密钥配置，生产部署门禁改为 `VERIFICATION_PROVIDER=disabled`，不填假密钥、不绕过门禁。
- 前端登录、管理员登录和找回密码界面移除手机号入口；邮箱仍为可选联系方式，昵称 + 密码可直接注册。
- 本地验证：12 个测试文件、81 项测试通过；TypeScript 构建通过。
- 生产发布仍待按备份、增量同步、Nginx/PM2、日志、health 和公网 smoke 顺序执行；“航线”与 Supabase 保持隔离。

## 2026-08-26（线上 `/dd/` 500 故障恢复）

- 故障表现：`https://dsxnb.com/dd/` 返回 Nginx 500，后端 `/dd/health` 仍为 200，说明故障位于静态站点入口而非 MySQL 或后端进程。
- 根因：服务器静态目录 `/var/www/dd` 缺失，Nginx 的 SPA 回退持续内部重定向到 `/dd/growth-school.html`，错误日志记录 `rewrite or internal redirection cycle`。
- 修复：从服务器保留的上一版前端备份恢复 `/var/www/dd`，校正目录属主；同时修正健康检查代理配置中的异常 `Host` 值，执行 `nginx -t` 后重载。
- 验证：带时间戳及 `no-cache` 请求的 `/dd/` 返回 200 和完整 HTML；`/dd/growth-school.html`、`/dd/health`、`/dd/blind-box/` 均返回 200；Nginx 配置有效且服务为 `active`，PM2 后端进程在线、健康请求返回 200。
- 经验：增量部署替换前端时不能先移除线上静态目录再假定上传成功；必须先上传到临时目录、核验入口文件存在，再原子切换目录，并将四个公网入口 smoke 作为部署完成门禁。

## 2026-08-26（商城前端暂时隐藏并上线）

- 主站新增单一商城展示开关 `data-shop-enabled="false"`，普通用户、商品发布者和管理员的 7 个商城导航入口及 7 个商城页面统一隐藏；直接调用商城页面导航会回到当前角色首页。
- 商城 DOM、交互函数、`api-client.js`、后端路由、统一 RBAC、Prisma 模型、迁移与测试均完整保留；未上传商城后端、未执行商城迁移，也未重启后端进程。后期只需启用同一开关即可恢复前端。
- TDD 验证：商城前端契约先出现 1 项预期失败，实施后聚焦前端回归 6 个文件/48 项通过；兑换码契约 3 项通过；`npm run build` 通过。
- 部署全程显式使用本机磁盘 Ed25519 私钥并禁用代理与密码认证。旧页面备份到 `/root/deploy-backups/growth-school.html.20260826-2202`，新文件校验 SHA-256 后原子替换 `/var/www/dd/growth-school.html`。
- 线上验证：Nginx 配置有效且服务为 `active`，`dandan-world` 为 `online` 且健康请求为 200；`/dd/`、`/dd/growth-school.html`、`/dd/health`、`/dd/blind-box/` 均返回 200。浏览器确认开关为关闭、14 个商城 UI 节点可见数量为 0。
- 已知既有问题：PM2 历史日志仍记录 `/api/notifications/unread` 与 `/api/tasks/mine` 的 404，和本次静态商城隐藏无关，后续后端迭代需单独处理。

## 2026-08-26（固定管理员登录修复）

- 根因：生产仍运行 8 月 25 日旧后端，数据库中不存在“蛋总-千焦”，旧服务也没有保护管理员字段和统一 RBAC，因此正确密码必然返回 `INVALID_CREDENTIALS`。
- 本地全量验证 30 个测试文件、163 项通过，TypeScript 构建通过；后端以 500 KiB 增量包上传，保留服务器依赖目录后更新依赖并重新生成 Prisma Client。
- 迁移前生成并校验数据库备份 `/root/backups/dandan_world/dandan_world-20260826142833.sql.gz`。仅部署 `202608260001` 至 `202608260006`，明确移出 `202608260007_egg_mall`，未创建商城表。
- 创建并保护“蛋总-敦敦”“蛋总-千焦”两个固定管理员账号；密码只以 bcrypt 哈希写入生产，账号状态为 `active`，统一管理员角色由服务启动 seed 授予。
- 线上验证：“蛋总-千焦”登录接口成功，`/api/users/me` 返回 `isAdministrator=true`、`isProtectedAdmin=true`、52 项权限；`mustChangePassword=false`，符合当前前端尚无强制改密界面的现实状态。
- PM2 已切换至 `/root/dandan-world-server-20260826-admin-fix/dist/src/server.js` 并保存，进程 `online`、0 次重启、错误日志为空；主页和 `/dd/health` 均为 200，商城前端开关仍为关闭。

## 2026-08-26（注册参数提示修复）

- 根因：前端只检查昵称和密码是否为空，却未落实后端的昵称 2-50 字、密码 8-128 位、邮箱格式和年龄 13-100 整数规则；无效值进入后端后统一显示“参数不符合要求”。
- 注册表单新增与服务端一致的提交前校验及输入属性，分别提示昵称、密码、邮箱和年龄问题；服务端校验保持不变，没有放宽安全边界。
- 首次上线检查发现浏览器复用了旧 `api-client.js`，HTML 改为加载 `api-client.js?v=20260826-registration-validation`，确保用户立即获得新校验逻辑。
- 验证：注册前端、服务端 validation 和注册会话共 33 项通过；前端契约复跑 22 项通过；`api-client.js` 语法检查和 `git diff --check` 通过。
- 线上 Chromium 验证：短密码、非法邮箱、非法年龄分别显示明确中文提示且不提交请求；主页和 `/dd/health` 为 200，PM2 `dandan-world` online，商城 UI 可见数量仍为 0。

## 2026-08-27（任务可见性与持久化第一阶段）

- 新增任务可见性领域规则：公开广场只查询 `approved`，审核队列只查询 `pending_review`、`needs_revision`、`rejected`，本人任务始终按登录用户 ID 查询。
- `Task` 新增 `skillCategory`、`skillSubcategory`，创建任务和待修改任务二次编辑均写入 MySQL；编辑后统一回到 `pending_review`。
- 新增公开任务、管理员审核队列、任务编辑和放弃任务接口；任务返回值包含真实 `activeClaimCount` 与当前用户 `claimStatus`，前端无需本地累加人数。
- 放弃任务写入 `TaskClaim.status=abandoned`，存在冻结金额时以幂等流水返还；内部 claims 数组不暴露给前端。
- TDD 记录：新用例先因缺少模块/路由和技能字段出现预期红灯；实现后任务可见性 3 项、持久化 5 项、既有业务 11 项全部通过，Prisma schema 有效，Prisma Client 生成与 TypeScript 构建通过。
- 经验：任务权限必须转换为服务端查询条件；人数和接取标记应随查询结果一次返回，不能由页面点击事件维护第二份状态。

## 2026-08-27（邀请码关系与首奖幂等）

- 新增 `Invitation` 关系模型，记录邀请人、被邀请人、邀请码快照、奖励时间和触发任务；被邀请人唯一约束阻止重复绑定。
- 注册事务校验邀请码状态并创建关系；无效、自邀和重复绑定返回稳定中文业务错误。
- 被邀请人第一个任务首次审核通过时，以 `rewardedAt IS NULL` 原子更新抢占奖励资格，只向邀请人发放一次 `20` 蛋蛋币并写入唯一幂等流水。
- 新增 `/api/users/me/invitations`，返回真实邀请码、邀请人数、奖励人数、累计奖励和逐条状态，不再依赖前端假历史。
- TDD 记录：模块缺失红灯后，邀请码领域与 HTTP 契约 6 项、认证会话 5 项、既有业务 11 项通过；Prisma generate、schema validate 和 TypeScript 构建通过。
- 经验：邀请奖励不能通过“先查再发”实现；必须在同一事务内先用条件更新抢占唯一资格，再更新账户和流水。

## 2026-08-27（生产数据清理、实时服务与正式发布）

- 本机完整回归通过：42 个测试文件、207 项测试全部通过；TypeScript 在本机完成构建，服务器未执行前端或 TypeScript build。
- 新增全站 WebSocket 通知通道，覆盖任务、打听、盲盒好友消息与排行榜变更；WebSocket 只发送失效通知，页面仍通过既有 REST 接口重新拉取业务数据，并保留断线重连和失败降级。
- Nginx `/dd/api/` 已增加 WebSocket Upgrade 转发，`nginx -t` 通过后重载；公网 `wss://dsxnb.com/dd/api/realtime` 实测握手成功。
- 实时路由设置 `logLevel: 'silent'`，避免查询参数中的短期访问令牌进入 Fastify 访问日志；修复前测试复现令牌日志，修复后测试与线上新增日志均确认不再记录 `/api/realtime?token=...`。
- 商城后端代码、RBAC、模型和接口继续保留，商城前端保持隐藏；生产未上传、未执行 `202608260007_egg_mall`。新增 `SHOP_ENABLED=false` 默认开关，关闭未建表环境中的商城定时维护，停止 `shop_orders` 不存在的周期错误。
- 只部署非商城迁移 `202608270001_task_skills_invites`，新增任务技能分类字段和邀请码关系表；迁移前确认生产库无对应字段，迁移后 Prisma 状态为最新。
- 执行单事务生产清理：精确保留“蛋总-敦敦”“蛋总-千焦”“教练”，删除其余 16 个测试/演示账号；清除旧蛋蛋币流水 22 条，将三人余额统一为可用 100、冻结 0，并为每人仅重建一条“初次上线 +100 蛋蛋币”流水。
- 两位固定管理员均通过生产登录接口实测为 HTTP 200；持久登录链路通过登录 Cookie、刷新令牌和 `/api/users/me` 三段验证。非法注册参数返回中文 HTTP 400，不再意外 500。
- 公网验收：`https://dsxnb.com/dd/growth-school.html`、`https://dsxnb.com/dd/health` 和公开 REST 均返回 200；PM2 `dandan-world` online、错误日志为空，未发现 MySQL 1045 或新 PID 的商城表错误。
- 回滚资料：`/root/dandan-backend-before-realtime-20260827-020029.tar.gz`、`/root/dandan-frontend-before-realtime-20260827-020029.tar.gz`、`/etc/nginx/conf.d/dsxnb.conf.before-realtime-20260827-020029`、`/root/dandan-db-before-cleanup-20260827-020029.sql.gz`。
- 部署全程使用指定磁盘 Ed25519 私钥及 `IdentitiesOnly=yes`，未调用 ssh-agent、密码登录、浏览器或 WebShell；未读取或修改“航线”项目和 Supabase。
- 经验：生产功能隐藏不等于后台任务停止，未迁移模块必须由默认关闭的运行时开关保护；增量包除业务文件外还要包含生产脚本的源码依赖，否则 `tsx` 会在事务开始前因模块缺失退出。

## 2026-08-27（隔离业务 E2E 与前端真实性清理）

- 前端移除默认技能、固定管理员 MBTI/满属性、固定余额/经验值、默认角色解锁和盲盒假用户注释；未删除真实接口或商城实现。
- `dandan_campus_test` 与 `dandan-world-test` 提供独立写入环境，未向生产库写入任何测试账号或 `[demo]` 记录。
- 新增可重复 E2E 脚本，固定连接本机 SSH 隧道 `127.0.0.1:13311`；覆盖注册、会话恢复、资料二次编辑、邀请奖励、任务、打听、盲盒、好友聊天、排行榜、三类实时事件和 REST 降级，结果 22/22 通过。
- E2E 报告统一脱敏 password/token/cookie；最终报告未检出临时密码或 JWT。
- 本机全量验证为 42 个测试文件、208 项测试全部通过，TypeScript 构建成功。

## 2026-08-27（生产三文件增量发布与复验）

- 通过本地和远端 SHA-256 清单确认生产后端业务构建无差异，因此保留远端 `node_modules` 与 `.env`，不执行服务器 build，也不无意义重启后端。
- 本机无 `rsync`，按备选方案生成只含 `growth-school.html`、`api-client.js`、`blind-box/app.js` 的增量包；上传后远端包哈希和三个目标文件哈希均与本机一致。
- 覆盖前备份到 `/root/deploy-backups/dd-before-frontend-cleanup-20260827-0310.tar.gz`；Nginx `-t` 通过后平滑重载，状态为 `active`。
- 公网首页、health、排行榜 REST 均为 200；`蛋总-敦敦`、`蛋总-千焦` 登录、管理员权限与 Cookie refresh 全部通过；WSS 握手成功；PM2 错误日志为空。
- 商城继续由 `data-shop-enabled="false"` 和 `SHOP_ENABLED=false` 关闭，代码保留，商城迁移未执行；“航线”项目与 Supabase 未触碰。
- 经验：增量部署前先用内容哈希确认真正变化范围，能避免重传后端、覆盖环境文件或重启无变化进程；部署完成必须以业务接口、认证、WSS 和日志共同验收。

## 2026-08-27（动态管理数据与空 JSON 500 修复上线）

- 管理员工作台、审核队列和用户列表改为服务端真实数据；个人技能、任务状态与打听发布完成后统一重新拉取 REST，不再使用本地假计数或假成功状态。
- 生产只读复核只存在“蛋总-敦敦”“蛋总-千焦”“教练”；每人仅有一条“初次上线 +100 蛋蛋币”流水。商城迁移仍未执行，前后端商城开关继续关闭。
- 生产验收捕获空 JSON 请求体被误报 500：根因是 `FST_ERR_CTP_EMPTY_JSON_BODY` 未进入既有 JSON 400 分支。回归测试先复现 500，再最小修复为 `400 INVALID_JSON`。
- 本机 42 个测试文件、215/215 通过，TypeScript 构建通过；独立测试库的完整 E2E 再次 22/22 通过。
- 仅上传后端 `dist/src/app.js` 与前端 `growth-school.html`，保留服务器 `.env`、`node_modules`，服务器不 build、不执行商城迁移。回滚文件位于 `/root/deploy-backups/dynamic-admin-20260827-033953`。
- 线上复验：health 200；两位管理员登录、`/me`、Cookie refresh 均为 200；管理员用户列表 3 人；审核队列接口 200；WSS 握手成功；PM2 online、Nginx active，新 PID 无 level 50 和 HTTP 5xx 日志。
- 经验：上线烟测工具本身也可能构造异常请求；看到 500 时必须先用服务端错误码定位边界，再判断是业务故障还是请求格式问题，同时仍应让所有客户端输入错误稳定落在 4xx。

## 2026-08-27（前端事实来源审计与协商取消上线）

- 角色/MBTI 选择与打听点赞、评论、回答、采纳统一改为“写 API 后重新拉取 REST”，清除写成功后前端自行伪造业务状态的路径。
- 教学任务协商取消新增 MySQL `task_cancellation_requests`：申请方和接收方受任务关系校验，48 小时窗口内可申请；接收方同意后由同一事务取消任务、更新接取记录、退回冻结蛋蛋币并写入通知/实时事件。
- 原发布者直接取消路径同步补齐教学任务认领者冻结金额返还，所有返还使用幂等流水键。
- 本机验证：43 个测试文件、220 项测试通过；TypeScript 构建、Prisma schema 校验和页面内联脚本解析通过；隔离真实写入 E2E 扩展至 23/23。
- 生产仅应用 `202608270002_task_cancellation_requests`，通过迁移记录确认商城 `202608260007_egg_mall` 未执行；保留商城前后端关闭开关。
- 部署后公网 health、管理员登录、新协商接口和 WSS 全部为 200/成功；PM2 `dandan-world` online，Nginx active，最新日志未发现 MySQL 1045、Prisma 初始化错误或 HTTP 500。回滚目录：`/root/deploy-backups/task-cancellation-production-20260827-041809`。

## 2026-08-27（任务事实来源第二轮审计上线）

- 确认配对与直接取消任务在接口成功后不再手工更新或删除任务卡，统一重新读取“我的任务”、公开广场和蛋蛋币流水；服务器通知继续是唯一的持久通知来源。
- 通知铃铛移除五分钟本地伪通知注入，写操作后的提示使用 toast，通知列表只从 `/api/notifications/unread` 获取。
- 公共任务 API 现在安全返回发布者最小身份 `{ id, nickname }`，不返回原始用户关系；任务卡据此渲染真实发布者并使用发布者 ID 判断本人任务。
- 任务详情删除固定“隐士蛋”头像、满属性和管理员身份兜底；缺少关系数据时明确显示资料不可用，绝不制造账号或统计数值。
- TDD：新增任务响应发布者、任务写入后回读、通知唯一来源和详情不伪造资料的回归用例，先红后绿。全量本机验证为 43 个测试文件、225 项通过，TypeScript 构建通过。
- 生产仅增量上传 `server/dist/src/app.js` 与 `backend-handoff-package/growth-school.html`，远端 SHA-256 与本机一致；未上传环境文件、未安装依赖、未执行商城迁移或服务器构建。回滚目录：`/root/deploy-backups/dynamic-task-state-20260827-0441`。
- 公网验收：`/dd/health` 为 200，匿名 `/dd/api/tasks` 为预期 401，Nginx active，PM2 `dandan-world` online，部署后错误日志为空。

## 2026-08-27（管理员发布任务描述必填上线）

- 管理员发布任务的描述改为必填；移除空描述时自动写入“蛋总发布任务”的开发期兜底，避免页面再产生硬编码示例任务内容。
- 本机验证完成：43 个测试文件、226 项测试通过，主页面两段内联脚本解析成功。
- 仅增量替换 `/var/www/dd/growth-school.html`，远端哈希与本地一致；未构建服务器、未执行依赖安装、数据库迁移或商城启用。
- 生产验收：`/dd/health` 返回 200，匿名 `/dd/api/tasks` 返回预期 401，Nginx active，PM2 `dandan-world` online，错误日志为空。回滚副本：`/root/deploy-backups/admin-task-description-20260827-045102/growth-school.html`。

## 2026-08-27（前端历史兼容路径复审）

- 复审主站、API client 与盲盒前端，未发现使用 `localStorage` 或 `sessionStorage` 持久化业务记录的路径；任务、打听、通知和资料关键写入继续以 REST 回读作为唯一事实来源。
- 全量本地回归：43 个测试文件、226 项测试通过；TypeScript 构建成功；主页面两段内联脚本、`api-client.js` 与盲盒 `app.js` 均通过语法检查。
- 本轮未发现需要修复或上线的新缺口，因此没有触碰生产文件、数据库、商城开关或“航线”项目。

## 2026-08-27（反馈工单前端控制器修复上线）

- 发现并修复反馈弹窗、反馈筛选、我的反馈历史、管理员回复弹窗等可见控件缺少前端处理函数的问题；此前点击会触发 `ReferenceError`。
- 新实现只调用既有反馈 API：用户提交后上传附件并回读历史；管理员按权限读取工单、筛选和回复；“待修改”状态由用户补充内容后重新回读。未新增本地业务存储或模拟数据。
- 全量本地验证：43 个测试文件、227 项测试通过，TypeScript 构建和页面脚本解析通过；静态可达性检查确认 78 个 API 调用与 180 个内联事件处理器均有实现。
- 仅增量发布 `/var/www/dd/growth-school.html`，远端 SHA-256 与本地一致。公网反馈控制器内容、health 200、匿名任务接口 401、Nginx active、PM2 `dandan-world` online、错误日志为空。回滚副本：`/root/deploy-backups/feedback-ui-20260827-050757/growth-school.html`。
- 随后校正“已回复”统计为服务端管理员消息数，而非处理中状态数；最终生产页面 SHA-256 为 `9900DE6A1E31175A927D3CCA9B5C291AC9AF6D9465DC307A6BB4AF1B772558E0`，最终回滚副本：`/root/deploy-backups/feedback-ui-label-20260827-051046/growth-school.html`。

## 2026-08-27（前端任务历史分支清理）

- 移除主站中已不可达的 DOM 本地造任务与本地删除任务兼容分支：`addToMyTasks`、`deleteMyTask`、`deleteGroup`。这些分支不调用后端，会生成固定进度、状态和联系方式，违背生产数据必须来自 REST 接口的规则。
- 真实任务流程保持不变：任务发布、认领、取消和广场/我的任务刷新仍统一调用既有 REST API 并回读服务端状态。
- 修正前端真实性契约的换行匹配，使其同时兼容 CRLF 和 LF；完整本机回归为 43 个测试文件、228 项测试通过，`npm run build` 通过。
- 商城保持前后端关闭；未修改数据库、迁移、服务器环境变量、“航线”项目或 Supabase。
- 已发布到 `/var/www/dd/growth-school.html`，本地与生产 SHA-256 均为 `7EE69EC6580208B4814791C02BF556B169BE3B0D272A7C69750A92FFF6939CA9`；回滚副本：`/root/deploy-backups/task-history-cleanup-20260827-052243/growth-school.html`。
- 公网复验：`/dd/health` 返回 200，匿名 `/dd/api/tasks` 返回 401，Nginx 为 active，`dandan-world` 为 online；当前进程最近 500 行无 5xx，错误日志为空。

## 2026-08-27（前端演示数据清理收口）

- 清理公共任务卡和任务详情中的固定发布者身份、稀有度、头像、等级、属性及隐藏管理员演示卡片；发布者只显示后端返回的最小真实资料。
- 清理角色、MBTI、声望、点赞、蛋蛋币和资料缺失时的固定默认值；简介保存后重新读取用户状态，不再维护本地公共资料副本。
- 盲盒抽取结果改为严格使用服务端 `result.action`；问答墙改为用户输入并调用既有 API；通知列表完全读取服务端未读通知，不再保留本地临时记录。
- 本机验证：43 个测试文件、233 项通过；`npm run build` 通过；主页面 2 段内联脚本与 `api-client.js`、盲盒 `app.js`、实时客户端语法检查通过；真实性禁入标记全部清零。
- 商城仍保持 `data-shop-enabled="false"` 与 `SHOP_ENABLED=false`，本轮未执行商城迁移，也未读取或修改“航线”项目与 Supabase。
- 已生产发布：仅更新 `growth-school.html` 与盲盒 `app.js`；本机、远端及公网下载哈希一致。主页面 SHA-256 为 `74350765DADC706DE28E728518BF946044E61CCC4AC1FC93A2376F3D343AF2ED`，盲盒脚本为 `658032A5288E3F64C8E3A80EA3CF39F29AA5CD9F760F84D101D5937B710636FD`。
- 回滚目录：`/root/deploy-backups/frontend-real-data-20260827-055608`。公网 health 200、匿名任务 401、Nginx active、PM2 `dandan-world` online；错误日志为空，当前 PID 日志无 level 50 或 HTTP 5xx。

## 2026-08-27（任务完成链路与运行时残留清理上线）

- 修复页面初始化调用已删除的客户端退款扫描器，避免访客首屏产生 `ReferenceError`。
- 发布者审核提交记录改为从任务认领 API 读取；确认完成调用真实 `/complete` 事务并刷新任务、蛋蛋币流水后，再进入真实评价接口。
- 删除无入口的组队本地延时完成/转账函数；邀请发布任务在服务端通道未开放时不再伪造通知，仅显示不可用提示。
- 本机全量验证：43 个测试文件、237 项通过；TypeScript 构建、外部脚本与主页面内联脚本解析通过，相关伪造标记全部清零。
- 生产仅增量更新 `/var/www/dd/growth-school.html`，本机、远端及公网哈希为 `8453F9225A7D96C7DAFC5009406D2D1D802BE113429C43A95BAC46C0B308573A`；回滚目录 `/root/deploy-backups/frontend-task-runtime-20260827-060937`。
- 公网 health 200、匿名任务 401、Nginx active、PM2 `dandan-world` online（PID 2477809），错误日志为空，当前 PID 无新增 level 50 或 HTTP 5xx。

## 2026-08-27（个人评价动态化与生产增量发布）

- 新增当前用户收到评价接口 `GET /api/users/me/ratings`，后端只按认证用户查询，返回平均分、评价数量、评价来源最小资料和关联任务标题；个人页删除空数组占位和前端声望写回。
- 任务提交审核每次读取真实认领记录；确认完成先调用服务端完成事务，刷新任务与蛋蛋币流水后再进入真实评价接口。页面初始化残留、伪邀请通知和无入口 DOM-only 转账逻辑均已清理。
- 本机完整验证为 43 个测试文件、239 项测试通过；TypeScript 构建、`api-client.js`、构建后端脚本、主页面两段内联脚本和差异检查通过。
- 发布前备份到 `/root/deploy-backups/profile-ratings-20260827-062009`；仅覆盖 `dist/src/app.js`、`api-client.js`、`growth-school.html`，服务器未 build、未安装依赖、未改 `.env`、未执行任何迁移。
- SHA-256：后端 `8B21BCED7CE9CD9E14DA49B79E3FC2B79E9BC14C191A40F95431E5AD0ABB77A2`；API 客户端 `3F1E6DB4F8E853276592529A0646EE8A965C6D3CBDD8C51DB76EF6EE316D6565`；主页面 `3E60941296DA73851CA934B2002453319EFA356457D41DFCB06D1DB0532F7EA0`。远端正式路径和两个公网前端文件均与本机一致。
- 生产验收：主页面 200、health 200、匿名任务 401、匿名评价接口 401；Nginx 配置有效且 active；PM2 新 PID `2506329` online、unstable restarts 0，错误日志为 0 字节，新 PID 无 level 50 或 HTTP 5xx。
- 商城继续保持前后端关闭，未执行商城迁移；“航线”项目和 Supabase 未读取或修改。生产已认证评价 200 未用明文凭据强行烟测，本地授权契约已在全量回归中覆盖。

## 2026-08-27（全站真实性第三轮审计上线）

- 任务广场初始数量不再显示固定 7/8/3；接口返回前为 0，返回后按真实任务数量更新。
- 用户名片按真实用户 ID 请求公开资料，未公开数据展示空状态；任务认领管理展示数据库返回的真实蛋蛋角色和稀有度。
- 清理九个无入口的旧任务 DOM 函数，任务发布、认领、取消、状态更新仍统一使用服务端 REST 写入和重新拉取。
- 本机完整回归为 43 个测试文件、244 项通过；TypeScript 构建、三份前端脚本、主页面两段内联脚本与差异检查通过。
- 仅增量发布后端 `app.js` 和主页面，回滚目录为 `/root/deploy-backups/production-truth-audit-20260827-071900`；服务器未 build、未安装依赖、未改环境变量、未执行迁移。
- 公网主页面与本机 SHA-256 一致，health 200、匿名任务 401、Nginx active、PM2 新 PID `2521240` online，错误日志为空且新 PID 无 5xx/数据库初始化错误。
- 商城前端和后端继续关闭，代码保留待后期无缝恢复；未触碰“航线”项目或 Supabase。

## 2026-08-27（隔离 E2E 自动凭据与隧道验收）

- 新增隔离管理员守卫与启动器，限定 `dandan_campus_test`、测试服务回环端口和固定隔离管理员标识；随机临时密码不落盘、不写入报告。
- 启动器使用指定磁盘 SSH 私钥和 `IdentitiesOnly=yes`、`BatchMode=yes` 建立 `127.0.0.1:13311` 隧道，结束时自动关闭隧道并恢复环境变量。
- 修复 Windows PowerShell 加密随机数 API 与远端 Node 20 stdin 读取兼容性；远端只补齐隔离测试目录的两个文件。
- 当前隔离真实写入回归 23/23 通过，覆盖认证、资料、邀请码、任务、打听、盲盒、好友聊天、排行榜、WebSocket 与 REST 兜底；详见 `docs/online-test-reports/2026-08-27-isolated-e2e.md`。
- 本机完整回归 44 个测试文件、249/249 通过，TypeScript 构建成功；十分钟持续回归任务首轮已复用启动器独立完成 23/23，结束后隧道自动关闭。
- 隔离回归后只读复验生产：`https://dsxnb.com/dd/health` 返回 200，Nginx active，PM2 `dandan-world` PID `2521240` 仍在线。服务器本地健康检查必须携带站点 `Host` 头或使用公开域名，避免落入默认虚拟主机造成假 404。

## 2026-08-27（好友模块与开源 RBAC 复核）

- 对历史好友模块问题逐项核验：拒绝冷却关系按最新更新时间选择；聊天接口读取最新 100 条并按时间正序返回；待处理好友申请只显示同意/拒绝；推荐接口空结果会清空旧卡片；收件箱昵称和消息均做 HTML 编码。
- 隔离真实写入 E2E 已覆盖好友申请去重、接受前禁止聊天和接受后私有实时消息，当前不需要修改好友业务代码或数据库迁移。
- 完成 GitHub 开源 RBAC 调研复核：继续使用现有 Prisma/MySQL 统一授权模型，不引入 Casbin Prisma adapter、OpenFGA、Permify、Ory Keto 或 Cerbos。完整依据见 `docs/research/2026-08-26-rbac-open-source-options.md`。

## 2026-08-27（认证资料、任务广场与消息角标上线）

- 个人页与侧边栏统一读取 `DandanAppState` 的认证用户，刷新恢复后昵称和学校保持一致，不再出现账号已登录但个人页显示“未登录”。
- 个人角色区改为真实数据角色护照；任务广场卡片展示后端返回的发布者昵称、蛋蛋角色、稀有度、经验、蛋蛋币、标签和认领进度，并保留原筛选及详情入口。
- 注册改为用户名和密码必填，邮箱可选且无需验证码；手机号注册入口取消。后端对重复账号、参数非法和数据库不可用返回中文业务错误。
- 五个消息数字统一由 `renderUnreadBadge` 渲染：数量为 0 时只隐藏数字，消息/审核/反馈入口容器继续显示。
- 清理演示数据时遗漏的 `GOSSIP_POSTS`、`currentGossipCat`、`currentGossipDetail` 空状态已补回，生产首屏不再触发对应 `ReferenceError`。
- 移动端反馈悬浮按钮移至 72px 侧栏底部，保留入口且不再遮挡任务卡。
- 本机完整验证：49 个测试文件、281/281 通过；TypeScript 构建、脚本语法和差异检查通过。Playwright 完成桌面与手机任务卡、角色护照及零角标状态检查。
- 生产增量发布：仅更新真实差异的前端文件、新增状态/身份/动效/Anime 资源，以及后端 `app.js`、`auth/validation.js` 和对应源码；保留 `.env`、`node_modules`，服务器未 build、未执行数据库迁移，商城继续隐藏。
- 三轮部署后检测全部通过：每轮隔离 E2E 23/23，合计 69/69；三轮生产 health 均为 200。PM2 `dandan-world` online、Nginx active、错误日志 0 bytes。详见 `docs/online-test-reports/2026-08-27-production-three-rounds.md`。
- “航线”项目和 Supabase 未读取或修改；商城代码保留待后期启用，前端入口仍关闭。

## 2026-08-27（资料刷新空节点修复）

- 根因：个人页改版移除了 `profileLevel` 展示节点，但 `refreshProfile()` 仍在登录、注册成功和多个资料刷新入口中写入该节点，导致 `Cannot set properties of null (setting 'textContent')`。
- 删除唯一过期写入；经验值继续由现存的 `profileExp2` 节点展示，不改变注册、登录、接口或数据库逻辑。
- 新增回归契约，禁止再次向未渲染的 `profileLevel` 节点写入。今后页面结构调整时，必须同步审查所有对应的 DOM 更新路径。
- 本机验证：定向契约 6/6 通过；完整测试与 TypeScript 构建通过。仅增量发布 `/var/www/dd/growth-school.html`，本地、远端和公网 SHA-256 一致：`F82A9970927B28B151C8AC3FD358A0D4664B724A25903E62A094BB413F249AC0`。
- 生产复验：`/dd/growth-school.html` 与 `/dd/health` 均为 200，Nginx active，`dandan-world` online，错误日志为空；回滚副本为 `/root/deploy-backups/profile-dom-null-20260827-1229/growth-school.html`。未重启后端、未改数据库、环境变量、商城、“航线”或 Supabase。

## 2026-08-27（项目交接资料）

- 新增 `docs/交接文档-2026-08-27.md`，记录当前生产拓扑、已上线功能、商城关闭边界、标准部署与回滚步骤、隔离测试策略、已知待办及本轮已验证的排障经验。
- 文档只记录变量名、路径、命令和哈希，不包含密码、令牌、验证码、私钥或环境变量值。


## 2026-08-27（打听邀请、技能变现与技能标签上限修复）

- 修复打听界面邀请发任务被大厅遮罩挡住：`.rank-modal-overlay` z-index 由 200 提升到 400，高于大厅遮罩 300。
- 开放服务端邀请通道：新增 `POST /api/task-invites`，校验技能 1-7 个、禁止邀请自己，写入 `Notification(type:'invite')` 并发布 `task.invited` 私聊实时事件；前端新增 `apiClient.sendTaskInvite`，`sendInviteTask` 改为真实异步调用，弹窗文案移除“服务端邀请通道暂未开放”。
- 修复技能变现“查看详情”无响应：`syncPublicTaskPlazas` 异步渲染任务卡后重新绑定 `.claim-btn` 并初始化发布者点击。
- 修复技能变现头像点不开：`initPublisherClick` 的头像选择器由 `.pub-egg-wrap` 改为新卡片 `.task-card-avatar`，并从 `data-publisher-id` 读取用户 ID。
- 修复技能标签保存不生效：`saveSkills` 改用 `window.apiClient.updateMe`（原先作用域引用了不存在的裸 `apiClient`）。
- 技能标签上限 7 个：前端 `toggleSkill`/`addCustomSkill` 增加数量拦截；后端 `profileUpdateSchema.skills` 由 `max(12)` 收紧为 `max(7)`。
- 验证：新增 `server/tests/task-invite-contract.test.ts`（6 项）与 `server/tests/task-invite-flow.test.ts`（3 项真实 HTTP 注入测试）；本机 `npm run build` 通过，完整测试 51 个文件、291/291 通过。
- 迁移状态复核：生产库 `prisma migrate status` 显示 12 个迁移全部已应用、schema up to date；本次修复无需新增迁移（邀请写入复用已存在的 `notifications` 表），商城 `egg_mall` 迁移按决策未上传/未应用。
- 隔离 E2E 最终门禁：重启 `dandan-world-test` 加载新代码后，在测试库 `dandan_campus_test`（端口 3311）复跑，25/25 通过；新增“邀请发布任务接口与通知持久化”“技能标签上限 7 个”两个场景。生产进程与生产数据未受影响。
- 生产增量发布：前端 `growth-school.html`（SHA-256 `73819EAF590ACB1541797BD03323AC1642D520D575E125B3D19B5377E6124BCE`）与 `api-client.js`（`A758AC0571A97A61AF2430598C7658C0FB447775E9F8E3534215D80890CBDA80`）；后端 `dist/src/app.js`（`51087D469A161697EF5887ED9BCCF5A9A65E312A16B50E7E758E240AB5BCDFB6`）、`dist/src/profile.js`（`2C7755C6588E3279FD4D73138F35E77D137D1AB19DC7EE4E5F2758C12836F9DD`）及对应源码；Nginx 重载，`pm2 restart dandan-world --update-env`。
- 发布后验收：`/dd/health` 200，页面 200，Nginx active，`dandan-world` online，错误日志为空；新接口未认证返回 401（鉴权生效）。回滚目录 `/root/deploy-backups/task-invite-20260827`。未执行数据库迁移，未触碰商城、“航线”、Supabase 及 `dandan-world-test`。

## 2026-08-27（敏感词误杀、邀请反馈与卡顿收口）

- 敏感词误杀修复：后端 `server/src/content-filter.ts` 与前端 `backend-handoff-package/sensitive-filter.js` 不再加载 5 万余条通用 GFW 词表，改为只拦截校园社区高信号滥用词（引流联系方式、色情博彩、刷单诈骗、代考办证假证、毒品枪支洗钱传销等）。`api-client.js` 的前端兜底词表同步对齐。
- 卡顿来源修复：页面加载不再同步构建 5 万余词 Trie，`sensitive-filter.js` 由约 574KB 缩减到约 2KB，消除首屏阻塞。
- 邀请对象一致性：`showUserEggModal` 拉取的公开资料 `skills` 统一为数组；邀请按钮对当前用户隐藏，避免向自己发送邀请；沿用上一轮已补的 `authorId`/`publisherId` 传参，邀请目标不再因缺少用户 ID 而丢失。
- 交互反馈：邀请发送按钮接入 `dandan-button-loading` 加载态与防重复点击；全局请求进度条继续覆盖所有异步请求。
- 验证：`npm run build` 通过；完整测试 51 个文件、292/292 通过。误杀回归确认“英语四六级”“教师资格证”“俄罗斯留学生”均放行，“加微信”“诈骗”“诈-骗”“代考”“办证”“卖淫嫖娼”仍拦截。
- 未执行数据库迁移；未触碰商城、“航线”、Supabase 与 `dandan-world-test`；本次为本地代码收口，尚未增量发布到生产。

## 2026-08-27（敏感词误杀与邀请反馈修复上线）

- 生产增量发布：前端 `growth-school.html`（SHA-256 `DF7CBE9F6FB4A5B9A4DCDA12C5C0D57237F98EC636104EE0367ED8973610108A`）、`api-client.js`（`5F0E04EF31A502DF6AC49A4C7A0FB1F1F74619AE03E7E9D1FD6D47BBC90D766D`）、`sensitive-filter.js`（`12DFE523907C9239E3ABACEFE1BA18A43A82191DE090C21C02476CD2F136FE25`）；后端 `dist/src/content-filter.js`（`AA2DD0B8724866128C014D5EB71ECE12723F2651F94D1A946D1A3A8BD657B23C`）与 `src/content-filter.ts`（`AE4167CC92F525183BD5118E3623D85F0D447E5ED03AD132E2EDC85F09EC338C`）。
- 发布前备份：`/root/deploy-backups/content-filter-curation-20260827-181929`（含前端三文件与旧 `content-filter.dist.js`）。
- 发布操作：`pm2 restart dandan-world --update-env`；未执行数据库迁移，未修改 Nginx 配置，商城、“航线”、Supabase 与生产库数据未触碰。
- 发布后验收：`/dd/health` 200；公网页面、`api-client.js`、`sensitive-filter.js` 均 200，且三个前端文件公网哈希与本机一致；`nginx -t` 通过；`dandan-world` online（新 PID `2704065`），错误日志为空；服务器端直读新过滤器确认“英语四六级/教师资格证/俄罗斯留学生”放行、“加微信/诈骗/诈-骗/代考”拦截。
- 隔离 E2E 门禁：`pm2 restart dandan-world-test` 加载新代码后复跑，25/25 通过，报告见 `docs/online-test-reports/2026-08-27-isolated-e2e.md`（测试库 `dandan_campus_test`）。
